# TODO / 今後の検討事項

運用ルール:
- 各項目にはファイル全体を通した連番 `#N` を振る(優先度セクションをまたいで通し番号)。
- 新しい項目は「これまでに使った最大の番号の次」から振る。現在の最大番号: 47
- 対応済みの項目は `CHANGELOG.md` に記載したうえで本ファイルから削除する。番号は欠番のまま残し、再利用しない。
- 優先度の見出しは項目が無くなっても削除せず、「現在該当する項目はありません。」と明記する。

## 優先度: 高

現在該当する項目はありません。

## 優先度: 中

- **#15** Android の PWA(WebAPK)で更新の反映が遅い問題。`vite-plugin-pwa` の `registerType: 'autoUpdate'` を使っているが、Android の WebAPK はプロセスを保持するため、デプロイ後に開き直しても古いキャッシュ(Service Worker がプリキャッシュした旧 JS)が動き続けることがある(実際、リンク intent 対応の診断表示が Android で全く出なかった＝新コードが動いていなかった)。Mac のブラウザでは概ね即反映される。対策案: (1)新バージョン検知時に「更新があります・再読込」トーストを出す(`vite-plugin-pwa` の `registerType: 'prompt'` + 明示リロード)、(2)アプリ起動/フォアグラウンド復帰時に SW の更新チェック(`registration.update()`)を明示的に叩く、(3)画面のどこかに常時ビルド版(コミットハッシュ)を小さく表示して、実機でどの版が動いているか一目で分かるようにする。動作確認は基本 Mac で行うため優先度は中。
- **#2** レイアウト切替機能。2026-07-12 のグリルで PC は「密度型(段積み3カラム)」を新しい定番として作り直す方針に決定(切替の仕組みは作らない)。まず密度型を数週間使ってから、単純リスト型3カラムなど別レイアウトへ切り替える必要があるかを判断し、必要なら設定画面から切替できるようにする。
- **#23** `--text-muted`(補助テキスト色)のコントラストが WCAG AA(通常文字4.5:1)を下回る。ライトは `#8c8175`(`index.css` 24行)で、surface 上 3.75:1・入力欄など bg 上 3.39:1(実測)。これを時刻・スニペット・リスト名・場所・期限・曜日見出しなど `--fs-xs`/`--fs-2xs`(11.5〜12.8px)の小さな文字に広く使う(`.gmail__snippet`・`.calendar__time`・`.tasks__due`・`.calendar__location` 等)。屋外のスマホや明るい会社 PC で判読困難。対策案: ライトの `--text-muted` を1〜2段暗くして 4.5:1 を確保(例 `#6f665b` 前後)。「読ませる補助テキスト」用の変数なので判読性を優先。(2026-07-12 レビュー・実測)
- **#24** 最重要ボタン(白文字×テラコッタ)のコントラストが不足、特にダーク。ダークの `--primary` は `#d9713f`(`index.css` 25行)で白文字との実測 3.29:1、ライトの `#c15a30` でも 4.4:1 と境界。「Google でログイン」「＋予定」「追加」「保存」等の 1rem 太字は WCAG の「大きい文字」に該当せず 4.5:1 が必要。対策案: ボタン背景用の色をテキスト用と分け、明暗とも白文字で 4.5:1 を超える濃度に調整。(2026-07-12 レビュー・実測)
- **#25** モーダル/ボトムシートのキーボード操作が未実装。`SettingsModal`・`EventSheet`(`CalendarPanel` 362行)・`EditSheet`(`TasksPanel` 474行)のいずれも、Esc で閉じる・フォーカストラップ(ダイアログ内でTabをループさせる仕組み)・開いた瞬間の初期フォーカス移動がない。`role="dialog" aria-modal` を宣言しているのに実挙動が伴わず、キーボード/スクリーンリーダー利用者は背後の body に Tab で抜けてしまう。対策案: 共通フックで (1)Esc→`onClose`、(2)開いたら最初の入力/閉じるボタンへフォーカス、(3)Tab をダイアログ内に循環、を実装。(2026-07-12 レビュー)
- **#26** Undo スナックバーがスマホの下端タブバーと重なり、かつ複数パネルで多重表示される。`.snackbar` は `bottom: 1.25rem` 中央固定(`index.css` 1235行)で、下端固定のタブバー(高さ約42px+セーフエリア)と重なって「元に戻す」とタブが競合する。さらに `CalendarPanel`/`TasksPanel`/`GmailPanel` がそれぞれ独立に `.snackbar` を描画・個別 state で持つため、別パネルの操作が続くと同じ位置に重なり最後の1件しか見えない。Undo は「見えて押せる」ことが安全網の前提。対策案: スマホでは `bottom` をタブバー高さ+`env(safe-area-inset-bottom)` 分持ち上げ、スナックバーを1つのトップレベル領域に集約して直近1件のみ表示。(2026-07-12 レビュー)
- **#27** ダークテーマでメール本文 iframe の背景が白固定で眩しい。`.gmail__frame { background:#fff }`(`index.css` 1214行)で、プレーンテキストは `var(--surface)` を使うのに HTML メールの iframe だけ常時白。加えて `buildSrcDoc`(`renderBody.ts` 165-178行)の本文リセットが `color:#111` 固定のため、単純に枠を暗くすると本文が黒地に黒文字で読めなくなる点に注意が必要。対策案: メール HTML の互換上いきなり全反転はせず、枠の余白・ローディング前背景をテーマ色にする、縁のギャップを緩和する等から始める(本文色の扱いとセットで検討)。(2026-07-12 レビュー)
- **#28** 取得失敗時に生の技術的エラー文字列をそのまま表示している。`予定の取得に失敗しました: {String(error)}`(`CalendarPanel` 227行)、同様に `TasksPanel` 278行・`GmailPanel` 146行で `String(error)`/`e.message` を露出。想定読者はプログラミング未経験で、英語の例外や `TypeError…` が出ても対処できず不安なだけ。空状態は平易な日本語なのにエラーだけ生表示で落差がある。対策案: ユーザー向けは「うまく取得できませんでした。再接続をお試しください」等の定型文にし、詳細はコンソール/折りたたみへ。401 等は「再接続」導線に寄せる。(2026-07-12 2人レビューで共通指摘)
- **#30** 予定作成シートで過去日を選べてしまい、作成後のフィードバックもない。日付入力(`CalendarPanel` 412-418行)は `type="date"` に `min` 指定がなく、過去日で作成しても一覧(今日以降)に出ない。5週間より先だけ通知がある(121-124行)のに過去は無言で、作った予定が消えたように見え混乱する。対策案: 過去日には「過去のため一覧には表示されません」スナックを出すか、`min` で当日以降に制限(過去予定作成の要件が無ければ後者が明快)。(2026-07-12 レビュー)
- **#31** 同時トークン要求がモジュール変数 `pendingResolve/pendingReject`(`gisClient.ts` 45-46行)を共有し、競合するとクロス解決/タイムアウトする。`doRequest` のたびに単一の resolver を上書きするため、起動時のサイレント認証(最大8秒)進行中に別経路の `connect()` が走ると、先発が resolver を奪われてタイムアウト reject に倒れる/後発が誤トークンで解決する恐れ。現状は `initializing` ゲート(`App.tsx` 64-91行)で概ね抑止されるが、コードで保証されておらず UI 導線が増えると露見する。対策案: 要求IDを採番して `Map<id,{resolve,reject}>` で対応付ける、または「保留中要求は同時に1つ」をコードで守る。(2026-07-12 レビュー)
- **#32** `calendarList`(カレンダー一覧)を複数クエリが各自フェッチしており共有キャッシュがない。`fetchUpcomingEvents`・`fetchEventDaysInRange`・`fetchWritableCalendars`・`useAccountEmail` がそれぞれ `calendarList` 系を叩くため、5分ポーリングのたびに二重取得が起きる(`calendar/api.ts`)。不要な API 呼び出し(クォータ・レイテンシ)に加え、取得ロジックが各所に散在し保守性が低い。対策案: `['calendar','list']` の共有クエリ(長め staleTime)を1本立て、予定取得・ドット・作成先一覧・アカウントメールがそれを参照する形にする。(2026-07-12 レビュー)
- **#33** `refetchOnWindowFocus`(前面復帰時の再取得)が `isMutating` でゲートされておらず、楽観的更新中の復帰で UI が一瞬巻き戻る。各クエリは `refetchInterval` を `useIsMutating()>0` で止めているが、グローバルの `refetchOnWindowFocus: true`(`queryClient.ts` 42行)は書き込み中でも発火する。`onMutate`(キャッシュ書換)と `onSettled`(invalidate)の間にフォーカスを得ると、サーバー未反映の旧データで楽観結果を一瞬上書きしてちらつく。対策案: 書き込み中は `refetchOnWindowFocus` も抑止する。(2026-07-12 レビュー)
- **#34** 進行中の複数日予定が「今後の予定」で過去日の見出しに出る。`fetchUpcomingEvents` は `timeMin=今日0時` でウィンドウに重なるインスタンスを返すため、昨日開始・明日終了の予定は実開始(昨日)を `startMs` に持つ。`CalendarPanel.groupByDay`(50-60行)は `startMs` 日付でグループ化するので、進行中予定が過去日見出しとして一覧最上部に現れ、`TodayTimeline` が今日0:00 にクランプして扱うのと不整合。対策案: 一覧側でも「今日にかかる進行中予定」は今日グループへ寄せる/過去日始まりの見出しを今日側に丸める。(2026-07-12 レビュー)
- **#35** README のセキュリティ方針「アクセストークンはメモリ保持のみ(タブを閉じると消える)」が実装と食い違う。実装 `tokenStore.ts`(54-56行)は `sessionStorage` にトークンを保存しページ更新時に復元しており、README 76行の記述(および XSS 対策の脅威モデル)とズレる。sessionStorage は XSS で読み出せる点がメモリ変数と異なるため、doc と code のどちらを正とするか一致させるべき。対策案: sessionStorage を採るなら README を「sessionStorage 保存/リロードで維持・タブを閉じると消える」に更新しトレードオフを明記、あるいは方針どおりメモリ保持のみに戻す。(#8 の sessionStorage 採用の記述とも整合を取る)(2026-07-12 レビュー)

## 優先度: 低

- **#3** 繰り返し予定のルール自体の編集(毎週→隔週など)への対応。初期バージョンでは Google カレンダー本家で行う運用。頻度が低いため UI の複雑さに見合うか使いながら判断する。
- **#4** Gmail の返信・新規作成機能。現状 Gmail を連絡用途に使っていないため未実装。必要になったら追加を検討する。
- **#5** 設定の端末間同期。現状は各端末の localStorage に個別保存(会社PCだけ Gmail オフにする等、端末別が都合良い面もある)。設定項目が増えて手動設定が煩雑になったら、Google Drive の隠し領域(appDataFolder)への保存を検討する。
- **#6** オフライン対応(データのキャッシュ)。Android の PWA インストール(WebAPK 化)に Service Worker が事実上必要だったため、`vite-plugin-pwa` による最小 Service Worker を導入済み(アプリシェル=HTML/JS/CSS のみプリキャッシュ)。これにより「アプリの枠は電波が無くても開く」ようになったが、**取得データ(予定・タスク・メール)のオフラインキャッシュは未対応**。移動中の閲覧ニーズが強ければ「最後に取得したデータのキャッシュ表示+『オフラインです』バナー」を追加検討する(書き込み操作のオフライン対応はしない)。
- **#7** PC 版のキーボードショートカット(R=更新、/=クイック追加 など)。初期バージョンは「Tab で移動でき、フォーカスが見える」最低限のみ。
- **#10** 複数日にまたがる予定の作成・編集UI。フェーズ4-c の予定作成/編集フォームは「単日」前提(日付フィールド1つ+開始/終了時刻)。既存の複数日予定を編集しても日数スパンは保持するが、新規に複数日予定を作る導線は無い。日跨ぎ・複数日の予定を作りたくなったら、フォームに終了日フィールドを追加する。頻度が低い想定のため後回し。
- **#9** タスクの時刻(時分)非対応の周知。Google Tasks 公開API(v1)は期限が日付のみで、時刻は読み書きできない(純正アプリで付けた時刻も公開APIには出ず、`updateTask` で期限を書き換えると日付のみのタスクに変わる)。ユーザーが「時刻付きタスク」を期待して混乱しうるため、(1)編集ボトムシートに「タスクは日付単位です。時刻が必要な予定はカレンダーへ」の一言を添える、(2)時刻付きにしたいときはカレンダー予定作成へ誘導する導線、などを検討。時刻が重要な項目はカレンダー予定で扱う、という設計の使い分けを UI で伝える。
- **#11** メール本文のインライン画像(`cid:` 参照)の表示。本文プレビューは HTML をサニタイズ+sandbox iframe で表示するが、メール内に埋め込まれた画像(`multipart/related` の `cid:` 参照)は表示せずプレースホルダに落としている(真面目に対応すると `attachments.get` で取得→`data:` URI 化→`cid:` 置換が必要でスライスが倍増するため割り切り)。埋め込み画像を見たいケースが増えたら対応する。
- **#12** メール本文の文字コード対応。本文デコードは UTF-8 決め打ち。日本語メールで多い `ISO-2022-JP`(いわゆるJISコード)等は化ける可能性がある。化けるメールが実際に出たら、パートの `Content-Type` の charset を見て `TextDecoder` の指定を切り替える対応を足す。
- **#14** Android でメール本文のリンクを Chrome 本体で開く(intent 方式)の実機検証。`IS_ANDROID` のときリンククリックを本文 iframe の外(本体側)で横取りし、`intent://…;package=com.android.chrome;S.browser_fallback_url=…;end` で Chrome 本体を起動する実装は入れてあるが、Android 実機で新コードが動いていなかった(#15 のキャッシュ問題)ため未検証。#15 が解消して新コードが動く状態で、(1)リンクが Custom Tab でなく Chrome 本体で開くか、(2)開かない場合は intent 起動側の調整(package 指定の要否、`window.location.href` 方式への変更、TWA 化の要否)を検討する。Mac/通常タブでは従来どおり `target="_blank"` で問題なし。
- **#13** メール添付ファイルの表示・ダウンロード。本文プレビューは本文テキスト/HTMLのみで、添付ファイル(PDF・画像等)は一覧も表示していない。添付を確認したいニーズが出たら、ヘッダのファイル名一覧表示→`attachments.get` でのダウンロードを検討する。
- **#17** 密度型レイアウトの24hタイムライン上でのフル操作化(残: Phase B/C)。Phase A(空き時間ドラッグ→時刻プリフィルで作成シートを開く)と予定クリックでの編集(旧 #18)は対応済み(2026-07-13、`CHANGELOG.md` 参照)。残るのは、予定の**上端/下端ドラッグでの時間帯変更(Phase B: リサイズ)**と、**予定本体ドラッグでの移動(Phase C)**で、これらができると Google カレンダー並みの操作感になる。実装・検証コストが大きい(タッチとパネル縦スクロールの競合、`useUpdateEvent` への楽観更新接続、前日/翌日にまたがりクランプ表示している端の扱い、`CalendarEvent → EventDraft` 変換の共通化など)ため、Phase A の使用感を見てから着手判断する。(2026-07-12 グリルで密度型レイアウトを合意した際に将来課題として切り出し・2026-07-13 に Phase A 対応で残課題を整理)
- **#16** RSS リーダー機能の追加。ニュースサイトやブログの更新(RSS/Atom フィード=サイトが配信する更新情報の一覧データ)をダッシュボードに取り込んで一覧表示する構想。未着手・要件未確定のため、まずは検討事項として記録。検討ポイント: (1)取得方式 — RSS はブラウザから直接取得しようとすると CORS(別ドメインのデータをブラウザが取りに行くのを制限する仕組み)で弾かれることが多く、`fetch` だけでは読めないフィードが多い。中継サーバー(プロキシ)や既存の変換サービスを挟むか、Cloudflare Workers 等の無料枠で極小プロキシを立てる必要があるか要検討(#8 の中継サーバー案と共通基盤にできる可能性あり)。(2)購読フィードの管理 — 登録URL一覧をどこに保存するか(端末ローカルの localStorage か、#5 の端末間同期の appDataFolder か)。(3)表示場所 — 既存の3カラム(予定/タスク/メール)に4つ目の列を足すのか、別レイアウト/タブにするのか(#2 のレイアウト切替と関連)。(4)既読管理・更新頻度など。優先度は低め、実運用が固まってから具体化する。
- **#8** 再ログイン頻度の対策。テストモード運用では「約1時間ごとに再接続1クリック+各端末で週1回フル同意」が発生する(`README.md` の「再ログインの頻度」参照)。なお、ページ更新でメモリが消えて毎回ログインになる問題は sessionStorage 採用で解消済み(同タブ内は維持)だが、1時間のトークン失効・7日の同意失効・新規タブ/タブ閉じ後の再ログインは残る。第1週の運用で苦痛と判明したら、(1) Cloudflare Workers 等の無料枠で動く極小の中継サーバー(トークン交換のみ)を追加して認可コードフローに移行、(2) アプリの本番公開(Gmail の制限付きスコープは審査が必要な点に注意)、のいずれかを検討する。
- **#36** ヘッダの設定歯車・モーダルの閉じる×のタッチターゲットが 44px 未満。`.app__settings` は約35px(`index.css` 196行)、`.modal__close` は `font-size:1.5rem` のみでパディングなし(1381行)。タスクのチェックは44pxに広げている(935-943行)のに歯車と×だけ小さく方針が不統一。対策案: 歯車を 2.75rem 角へ、×は透明な44px当たり判定を持たせる(タスクチェックと同じ擬似要素手法を流用)。(2026-07-12 レビュー)
- **#37** 空状態・エラー文言の句読点と語調が不統一。「今後の予定はありません」(句点なし・`CalendarPanel` 230行)/「タスクはありません。すべて順調」(`TasksPanel` 281行)/「受信トレイにメールはありません」(`GmailPanel` 149行)/「天気の取得に失敗しました。」(`WeatherPanel` 21行)で末尾句点の有無・トーンがばらつく。対策案: 「文末は句点を付ける」等のルールを1つ決めて統一し、空状態は前向きな一言で語調も揃える。(2026-07-12 レビュー)
- **#38** Undo スナックバー本文が1行省略で、長いタイトルが切れる。`.snackbar__text` は `white-space:nowrap`+省略記号(`index.css` 1252行)で、「「(長いタスク名)」を削除しました」形式(`TasksPanel` 150行等)だと何を取り消すのか読み取りにくい。対策案: 2行まで許容(`-webkit-line-clamp:2`)にするか、操作種別(削除/完了/移動)を先頭に置く。(2026-07-12 レビュー)
- **#39** 月ミニカレンダーのローディングだけテキスト表示で不統一。`MonthCalendar` は `読み込み中…`(105行)で、他パネルはスケルトン(`ListSkeleton` 等)。しかもグリッド描画済みの下にテキストが出る二重表示。対策案: セル内をスケルトン化するか、初回だけ淡いプレースホルダーに統一。(2026-07-12 レビュー)
- **#40** タブ/グリッドの ARIA 関連付けが不完全。`App.tsx` の `role="tab"`(250行)に対応する `role="tabpanel"`・`aria-controls`/`aria-labelledby` が各パネルに無く、非選択パネルは CSS で隠すだけで `aria-hidden` も付かないため、スマホのスクリーンリーダーが3パネルすべてを読み上げる。`MonthCalendar` の `role="grid"`(48行)配下にも `role="row"`/`role="gridcell"` や曜日ヘッダの role が無く、グリッドとして正しく解釈されない。対策案: `.panel--active` に `role="tabpanel"`+id を付け `aria-controls` で結ぶ。グリッドは row/gridcell を付与。(2026-07-12 2人レビューで共通指摘)
- **#41** 「今後の予定」に今日が含まれ、密度型で「今日の予定」タイムラインと重複する。見出しは「今後の予定」(`CalendarPanel` 144行)だが `useCalendarEvents` は今日を含むため、密度型では同じ今日ぶんがタイムライン(`App.tsx` 198行)と一覧の両方に出る。見出しの語(今後)と中身(今日含む)も不一致。対策案: 見出しを「予定」「これからの予定」等にし、密度型では一覧を明日以降に絞るなど役割分担を明確化(レイアウト自体は #2 の対象なので文言/範囲の整理のみ)。(2026-07-12 レビュー)
- **#42** 破壊操作の確認方針が未明文化で混在して見える。予定・タスクの削除は Undo スナックのみで確認なし(設計判断として妥当)だが、ログアウトだけ `window.confirm`(`SettingsModal` 36-41行)。対策案: 「Undo 可能＝無確認/不可逆＝確認」と方針を明文化し、それに沿っているか点検する(現状ほぼ沿っているので基準の言語化が主)。(2026-07-12 レビュー)
- **#43** メール本文の sandbox iframe が `allow-same-origin` を持ち、安全性が「`allow-scripts` を付けないこと」の1点に強く依存している。`GmailPanel` 314-321行の iframe は本体オリジンと同一の `srcDoc` を流し込むため、将来 `allow-scripts` が同時に付くと本体オリジンへの完全な XSS になる(CSP も `style-src 'unsafe-inline'` を許可)。`allow-same-origin` は高さ計測・Android リンク横取りで `contentDocument` を読むために付いている。対策案: sandbox 属性直上に「`allow-scripts` を絶対に追加しない」旨の強い警告コメントを置く。可能なら高さ計測を `postMessage` 化して `allow-same-origin` を外せないか検討。(2026-07-12 レビュー)
- **#44** 楽観的追加の仮 ID `temp-${Date.now()}`(`useInsertTask` 99行・`useCreateEvent` 45行)が同一ミリ秒の二重追加で衝突しうる。実害はほぼ無いが `crypto.randomUUID()` かカウンタ併用が堅牢。(2026-07-12 レビュー)
- **#45** Gmail のミューテーションだけ行コンポーネント `GmailRow`(182-185行)内に定義されており、カレンダー/タスクがパネル親に集約しているのと不統一。アーカイブで行がアンマウントされた後の Undo は現状 `notify` のクロージャで動くが脆い。対策案: 親コンポーネントへ引き上げて一貫させる。(2026-07-12 レビュー)
- **#47** 日付文字列の生成が2系統あり紛らわしい。`CalendarPanel.groupByDay`(54行)の React キーはゼロ埋めなし(`2026-7-5`)、一方 `data-date`(239行)や月カレンダーのスクロール要求はゼロ埋め(`2026-07-05`)。現状キーは React 用途のみで実害はないが、`fmtLocalDate` に統一するのが安全。(2026-07-12 レビュー)
