# 作業状態

## 現在の作業テーマ

別PC で作成したダッシュボード「AB Operations」を **このPC(c:\Claude\Tools\ABOperations)でも編集できるようにセットアップ**し、`TODO.md` 項目とユーザー要望の UI 改善を継続対応して GitHub Pages へ随時デプロイしている。2026-07-13 時点で **依頼分はすべて完了・デプロイ済み**（次の指示待ちの一区切り状態）。

## 進捗

| 項目 | 状態 |
|------|------|
| リポジトリ clone（`https://github.com/sabe1991/ABOperations`）| ✅完了 |
| Git ユーザー設定（`Shunya Abe` / `shunya.abe@gmail.com`）| ✅完了 |
| GitHub 認証（Git Credential Manager、アカウント `sabe1991`）| ✅完了 |
| Node.js 導入（v24.18.0・ユーザー領域・PATH 追加）| ✅完了 |
| TODO #17 Phase A / #18 / #29（タイムライン操作・スクロール）| ✅完了・デプロイ済 |
| タイムライン＝月カレンダー選択日を表示（見出し動的化）| ✅完了・デプロイ済 |
| TODO #23/#24/#27/#30/#34/#7/#10（＋#47 副次解消）| ✅完了・デプロイ済 |
| TODO #31/#32/#12/#36/#37/#38 | ✅完了・デプロイ済 |
| 各パネル右上の外部リンクボタン・ヘッダー中央の日付 | ✅完了・デプロイ済 |
| 予定選択で詳細/編集シート・終日チップ上部固定 | ✅完了・デプロイ済 |
| TODO #41（今後の予定に今日を含む）| ✅現状維持で決着（TODO から削除）|
| TODO #6（オフライン対応）| ⏸ 対応不要とユーザー判断（見送り）|

## 直近の変更・結論

- **最新デプロイ**: コミット `53e41e5`（「今後の予定は選択で詳細/編集シートを開く＋終日チップ上部固定」）。GitHub Pages 反映確認済み。
- **デプロイ手順**: `git commit` → `git push origin main` → GitHub Actions が自動ビルド&デプロイ。反映確認は公開 JS（`https://sabe1991.github.io/ABOperations/` の `assets/index-*.js`）に短縮コミットハッシュ（`__COMMIT_HASH__`=`git rev-parse --short HEAD`）が含まれるかをポーリングして行う。
- **ビルド/検証**: `npm run build`（`tsc -b && vite build`）で型チェック＋本番ビルド。Node は `C:\Users\n3728\tools\node-v24.18.0-win-x64`（PowerShell で `$env:Path` に前置して実行）。
- **主要な設計判断**:
  - #41 は「今後の予定に今日を残す」（タイムラインが今日以外を表示しうるため一覧で今日を見られるようにする）。重複はデザイン上の許容として決着。
  - #6 オフライン対応は今回見送り（依存追加・データ永続化の privacy 検討を伴うため、ユーザー判断で不要）。
  - #32 で `calendarList` を共有クエリ `['calendar','list']`（`useCalendarList`）に集約。予定/月ドットは一覧を引数受け取り、作成先一覧/アカウントメールは `select` で導出。
  - #31 で GIS トークン要求を「同時に1つ」直列化（`gisClient.ts` の `doRequest`）。interactive 要求はタイムアウト無し（正規の長いログインを切らないため）が意図的トレードオフ。
- **運用ルール**: コード変更のたびに `CHANGELOG.md` へ追記、対応済み TODO は `CHANGELOG.md` 記載後に `TODO.md` から削除（番号は欠番・再利用しない）。現在の最大番号: 47。

## 次のアクション

- [ ] 次のユーザー指示を待つ（本ファイル・`CHANGELOG.md`・`TODO.md` を読んで現在地を把握してから着手）
- [ ] （任意・低優先）展開UI撤去で孤立した未使用 CSS `.calendar__recur-note` を掃除
- [ ] （任意）残 TODO の対応（`TODO.md` 参照。中: #15/#2/#25/#26/#28/#33/#35 など）
