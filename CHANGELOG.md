# 変更履歴

## Added

- メール本文モーダルで、**添付ファイルのダウンロード一覧を本文の上に表示**するようにした(ユーザー要望)。従来は本文の下に置いていたため、長い本文では添付を落とすのに最後までスクロールする必要があった。`MessageBody` の描画順を「添付一覧 → 本文」に入れ替え、`.gmail__attachments` の上罫線が操作ボタン列(返信・未読・アーカイブ)との区切りとして機能するようにした (2026-07-24)
- ニュースパネルのソースに **dev.to**(英語の技術記事)と **GitHub**(直近1週間で星を集めたリポジトリ)を追加し、選べるソースを全7種にした(ユーザー要望)。いずれも従来同様 **APIキー不要・CORS 対応・JSON 直取り**で、`NEWS_SOURCES` にキー・表示名・説明を足し `fetchNews` に取得処理を足すだけの既存の拡張手順に沿っている(設定画面のチェックボックスとタブは一覧から自動生成されるため他の変更は不要)。dev.to は `dev.to/api/articles` を未認証取得(Qiita の英語版のような位置づけ。いいね数=`positive_reactions_count`・コメント数・投稿者を Qiita と同じ形で表示)。GitHub はトレンド用の公式APIが無いため検索API(`api.github.com/search/repositories`)で `created:>(1週間前)` を星の多い順に並べて代用し、リポジトリ名だけでは内容が分からないのでタイトルに説明文を続け(CSSで2行に省略)、▲に星数・提供元の位置に主要言語を出す(鍵なしだとIPあたり 60req/h だがパネルの更新頻度なら足りる)。なお候補として検証した **Zenn**・**Lobsters** は JSON を返すが CORS 許可ヘッダが無く、**Reddit** の `.json` は 403 で、いずれもブラウザからの直取りができないため見送った(中継サーバーが必要になり「直取りのみ」の方針と合わないため) (2026-07-15)

- メール一覧・本文で**スター（重要マーク）を付け外し**できるようにした(ユーザー要望)。本文モーダルの操作列に「☆ スター / ★ スター付き」トグルを置き（押してもモーダルは閉じず付け外しを続けられる）、一覧では付いているメールに金色の★を表示する(表示のみ)。実装は Gmail の `STARRED` ラベルの付け外し(`starMessage`/`unstarMessage`)で、既読化・アーカイブと同じ楽観的更新(`useToggleStar`。スターは並び順・在否を変えないので並べ替えはしない)。付け外しはトグルで戻せるため Undo スナックバーは出さない。モーダルへ渡すメールは一覧キャッシュの最新値にし、スター状態がボタンへ即反映されるようにした (2026-07-15)
- メール本文の**外部画像を既定で表示**するようにし、設定でON/OFFを切り替えられるようにした(ユーザー要望)。設定モーダルの「この端末の表示」に「メールの外部画像を表示」トグル(`abops:showExternalImages`、既定ON)を追加し、OFF にすると従来どおりブロックして本文の「画像を表示」で都度解禁できる。既定ONだと外部画像の自動読み込みで送信者に開封(時刻・IP＝おおよその所在地など)が伝わりうるため、設定に注意文を添え、あわせて画像取得時に参照元URL(アプリのURL)を送らない `<meta name="referrer" content="no-referrer">` を本文iframeに追加してハードニングした。埋め込み画像(`cid:`)はこの設定に関わらず常に表示(外部通信を伴わないため) (2026-07-15)
- **#4** メールの**新規作成・返信**ができるようにした。Gmail パネル見出しに「✎ 作成」ボタン、開いたメールの操作列に「返信」ボタンを追加し、宛先・Cc・件名・本文のボトムシート(`ComposeSheet`)から送信する。送信は RFC 2822 の MIME を組み立て `messages.send` へ base64url で渡す(`compose.ts` の `sendMessage`。本文は `text/plain; charset=UTF-8` を base64、日本語件名は RFC 2047 の `=?UTF-8?B?…?=` エンコードで MIME 全体を ASCII 化して `btoa` で符号化)。返信は差出人(`Reply-To` があれば優先)宛・件名 `Re:`・元本文の引用をプリフィルし、`In-Reply-To`/`References`/`threadId` で同じ会話にぶら下げる(`fetchReplyRefs` で元メールの Message-ID・References・Reply-To・生件名を取得。スレッド化メタの取得完了までは送信ボタンを無効化)。ヘッダ値は CR/LF を除去してヘッダ注入を防ぎ、宛先の日本語表示名は表示名部分だけをエンコードしてアドレス本体は残す。送信は `gmail.modify` スコープで可能なため追加同意は不要。認証切れ(401)・権限不足(403)は既存の共通ハンドラ(`MutationCache`)に流れる (2026-07-14)
- **#13** メール本文の**添付ファイル(PDF・画像等)の一覧表示とダウンロード**に対応した。`payload` を辿って添付パーツ(`attachmentId` を持ち、本文にインライン表示されないもの)を集め(`collectAttachments`)、本文の下にファイル名・サイズ付きで一覧する(`AttachmentList`)。行をタップすると `attachments.get` で実データを取得し、`Blob`+一時 `<a download>` でブラウザ保存を起こす(`fetchAttachmentBytes`)。除外するのは `Content-Disposition: inline` のインライン部品(署名ロゴ等・#11 で本文内に表示済み)だけにし、Outlook 等が通常添付に付ける `Content-ID` で添付が消えないようにした。ファイル名の無い添付には MIME から代替名を補う。ダウンロード経路は TanStack Query を通らないため、認証切れ(401)は手動で共通の再接続 UX(`markExpired`)に連携する (2026-07-14)
- **#3** 繰り返し予定の**繰り返しルール自体(毎週→隔週など)をアプリ内で編集**できるようにした。繰り返し予定の編集シートに「🔁 繰り返しルールを編集…」を追加し、マスター予定の `RRULE` を読み込んで頻度(日/週/月/年ごと)・間隔・終了条件(なし/回数/日付)を編集してシリーズ全体に適用する(`recurrence.ts` の `parseRecurrence`/`buildRecurrence`、`RecurrenceSheet`、`useUpdateRecurrence`)。上部の各項目が「この回のみ」に効くのに対し、これはシリーズ全体を変える別操作であることを注記で明示。安全策として、(1)このアプリが安全に往復できる `FREQ/INTERVAL/COUNT/UNTIL` 以外のパラメータ(`BYDAY` 等の曜日指定など)を持つ予定は「編集不可(本家で編集)」として扱い、保存で条件が黙って欠落するのを防ぐ、(2)`UNTIL` は終日=DATE / 時刻あり=UTC 日時に値型を DTSTART へ合わせ、ローカル終端をUTC変換してTZズレで前後の回が混ざらないようにする、(3)`recurrence` の差し替えは `RRULE` 行だけに限定し `EXDATE`(除外日)/`RDATE` を温存する、を入れた。ルール変更後はインスタンスが総入れ替えになるため一覧・月ドットのキャッシュを無効化する (2026-07-14)
- タスクパネルで、**タスクをドラッグして期限を変更**できるようにした(ユーザー要望)。行を掴んで「今日」「明日」「期限なし」のグループへ落とすと、その期限に変わる(HTML5 ネイティブ ドラッグ&ドロップ)。日付が一意に定まる3グループだけをドロップ先にし、複数日を含む「⚠期限切れ」「以降」は**そこから出せる(ドラッグ元)がそこへは入れられない**一方通行にした(何日にすべきか定まらないため)。実装: `useUpdateTask` の楽観的更新をそのまま流用(落とした瞬間に行が移動)、期限が変わらない同値ドロップは無視、変更後は「元に戻す」スナックバー(5秒)で取り消せる。ドラッグ中だけ空のドロップ先を破線枠のプレースホルダで表示し、入れられないグループは減光、乗っている先は枠でハイライトする。`draggable` は行を包む `div` に付け、クリック編集・完了チェックとは競合しない(追加直後 pending の行はドラッグ不可)。キーボード/タッチ・Firefox では従来どおり編集シートのチップで期限変更できる(HTML5 DnD が効かない環境の代替) (2026-07-13)
- 月ミニカレンダー(`MonthCalendar`)を**矢印キー(十字キー)で日移動**できるようにした(ユーザー要望)。`role="grid"` に WAI-ARIA のグリッド操作を実装し、←→で前後日・↑↓で前後週・Home/End で行内の端へフォーカスを移す(ロービングタブインデックス＝選択中セルだけ Tab 順に入れ、他は矢印キーで辿る方式)。**矢印キーで表示日(タイムラインの対象日)を即時に切り替える**(ユーザー要望)。ただし予定リストの自動スクロールはしない(1押下ごとにスクロールすると画面が暴れるため。クリック時のみ従来どおりその日へスクロール)。クリック不可の過去日はフォーカス対象から除外し、日をまたいで起点セルが過去化しても今日へ寄せる (2026-07-13)
- **#15** PWA(ホーム画面から起動できるアプリ版)の**更新をユーザーに促す方式**へ変更した。従来の `registerType:'autoUpdate'`(自動置換)は Android の WebAPK でプロセスが保持されると古いキャッシュのまま動き続け、新版へ切り替わったか分かりにくかった。`registerType:'prompt'`+自前登録(`src/pwaUpdate.ts` の `registerSW`、`injectRegister:null` で二重登録を防止)にし、(1)新版検知時に画面上部へ「新しいバージョンがあります・再読み込み」トースト(`UpdateToast`)を出して明示適用、(2)フォアグラウンド復帰(`visibilitychange`→visible)時に `registration.update()` を叩いて更新チェック、を追加した。ビルド版(コミットハッシュ)表示は既存の設定「情報」タブに続投 (2026-07-13)
- **#11** メール本文の**インライン画像(`cid:` 参照＝メールに同梱された埋め込み画像。署名ロゴ等)を表示**するようにした。`payload` を辿って `Content-ID` を持つ画像パーツを集め、`attachments.get` で取得して `data:` URI(画像データを本文に埋め込んだ形式)へ変換し、本文HTML内の `cid:` 参照(img の `src` と style の `background:url()`)を DOMParser 経由で置換する(`fetchMessageBody`/`collectInlineImages`/`replaceCidReferences`、1通あたり最大20枚)。`data:` 画像は外部通信を伴わず開封トラッキングの心配が無いため、外部画像ブロックの対象外として既定表示にする(`renderBody` の `afterSanitizeAttributes` で `data:` の `src` は退避しない)。個別取得の失敗は握り、その画像は従来どおりブロック表示に留める (2026-07-13)
- **#17**(Phase B) 密度型24hタイムラインで、**予定を直接ドラッグして時間帯を変更**できるようにした(マウス/ペンのみ。タッチは従来のタップ編集を維持)。予定本体のドラッグで時刻を平行移動、下端付近(`RESIZE_GRIP_PX`)のドラッグで終了時刻をリサイズし、離した時点で `useUpdateEvent` の楽観的更新で確定する(`CalendarEvent→EventDraft` 変換を `eventToDraft` に共通化)。対象は当日内で完結する時刻付き予定のみ(日をまたぐ・終日は除外)、終了は 23:45 までにクランプして 24:00 の無効時刻を避ける。微小移動(`EVENT_DRAG_THRESHOLD_PX`)未満はクリック(編集シートを開く)として弁別。ドラッグ可能な予定にはグリップ(`.timeline__event-grip`)と `grab`/`ns-resize` カーソルのアフォーダンスを付けた(#61) (2026-07-13)
- **#25/#55/#40** モーダル・ボトムシート・タブの**キーボード/スクリーンリーダー操作**を一括で整えた。(1)共通フック `useDialog` で、Esc で閉じる・Tab をダイアログ内に循環(フォーカストラップ)・開いた瞬間の初期フォーカス移動・背面のスクロールロック(`body { overflow:hidden }`)・閉じたら元の要素へフォーカス復帰、を `SettingsModal`/`EventSheet`/`EditSheet` に付与(スマホはキーボードで隠れないよう初期フォーカスをコンテナに寄せる)。(2)共通ヘルパ `roving.ts` で、スマホ下部タブ・設定タブ・ニュースソースタブに矢印キー移動(roving tabindex＝選択タブだけ Tab 順に入れる方式)を実装。(3)`role="tabpanel"`/`aria-controls`/`aria-labelledby` でタブとパネルを関連付け、月カレンダーに `role="row"`/`gridcell`/`columnheader` を付与 (2026-07-13)
- **#67** 予定・タスクの**空状態に作成 CTA(行動を促すボタン)**を追加した。「今後の予定はありません」等の説明だけだった空欄に「＋ 予定を作成」「＋ タスクを追加」を控えめに添え、そこから直接作成フローへ入れるようにした(`.empty-state`) (2026-07-13)
- **#60** 設定「情報」タブに**キーボードショートカット一覧(PC)**(R=全更新・`/`=クイック追加へフォーカス・Esc=閉じる)を追加し、`<kbd>` で見つけやすくした(従来は実装済みだが画面上に案内が無かった) (2026-07-13)
- タスクの**編集ボトムシートから「✓ 完了」でタスクを完了にできる**ようにした(ユーザー要望)。従来は行左の丸チェックからのみ完了でき、編集シートを開くと完了操作ができなかった。シート下部の左側に「完了(緑)・削除(朱)」のライフサイクル操作、右側に「キャンセル・保存」のフォーム操作を並べる。完了すると行がリストから消え、画面下部の「元に戻す」(5秒)で取り消せる(既存の完了フローを再利用)。狭い画面でもボタン文言が折り返さないよう `.sheet__buttons` のボタンを `white-space:nowrap` にし、ラベルは「✓ 完了」に短縮(360/320px 幅で1行に収まることをヘッドレスで確認) (2026-07-13)
- 高優先レビュー(UI/UX＋シニアエンジニア)の指摘に対応し、取得エラーやクラッシュに強くした。(1)**#49** 各パネル(タイムライン/予定/月カレンダー/タスク/天気/メール・ニュース)を React のエラーバウンダリ(`ErrorBoundary`)で個別に包み、想定外レスポンス等で描画が例外を投げても**アプリ全体を白画面にせず、その欄だけ「問題が発生しました＋再試行/再読込」表示に留める**ようにした(従来は1つの例外で `main.tsx` ごと落ちて Service Worker のキャッシュで復旧しにくかった)。(2)**#52** 予定・タスク・メール・ニュースの取得失敗表示に**「再試行」ボタン**(`refetch`)を追加し、ページ全体を再読込しなくても復帰できるようにした(共通 `PanelError`。エラーバウンダリと同じ `panel__note--error` 枠で見た目を統一)。(3)**#51** ヘッダーに**常設の更新ボタン**(↻・取得中は回転＋無効化・`queryClient.invalidateQueries`)を追加し、R キーの無いスマホでも全データを手動更新できるようにした。あわせて 640px 以下で消えていた最終更新時刻を、「最終更新」の語だけ隠して**時刻はスマホでも残す**ようにした(回転は `prefers-reduced-motion` で停止) (2026-07-13)
- **#48** セキュリティ強化として `index.html` に **Content-Security-Policy(meta)** を追加した。アクセストークンを `sessionStorage` に保持しメール本文の HTML も扱う構成のため、万一の DOM-XSS でも外部送信(トークン持ち出し)を抑えられるよう、スクリプト実行元を自オリジン＋Google 認証(GIS)に、通信先(`connect-src`)を実際に使う API ホスト(Google API・Open-Meteo・Qiita・Hacker News・気象庁・Spaceflight・Wikipedia)だけに絞った。ビルド後の `index.html` はインライン script が無い(GIS も SW 登録も外部 `src`)ことを確認し、meta を全 script/link より前に置いた。メール本文 iframe(srcdoc)の「画像を表示」を壊さないよう `img-src` に `https:` を含める。※meta 版 CSP は report-only 不可のため、実ログイン込みの動作は実機での確認が必要 (2026-07-13)
- ニュースパネルの**ソースを増やし(全5種)、設定画面から最大3つを選んでタブで切り替えられる**ようにした(ユーザー要望)。追加ソースはいずれも従来同様 **APIキー不要・CORS 対応・JSON 直取り**で、技術系に偏らないよう日本語・一般教養・科学系も混ぜた。カタログ: **Qiita**(日本語の技術記事)/**Wikipedia 注目**(日本語版のいま話題の記事=秀逸記事+アクセスの多い記事)/**地震情報**(気象庁の最近の地震)/**Hacker News**(英語の技術)/**宇宙ニュース**(Spaceflight News・英語)。実装: (1)ソース一覧を単一の真実として `news/newsSource.ts` の `NEWS_SOURCES`(キー・タブ名・説明)に定義し、`NewsSource` 型をそこから導出。取得は `news/api.ts` の `fetchNews` が共通形 `NewsItem` へ正規化(Wikipedia は REST の featured フィードで前日UTC分を取得しタイトル重複を除去・閲覧数をポイント表示、地震は気象庁 `bosai/quake/data/list.json` から震源地・M・最大震度を1行に整形し同一地震の続報を集約、宇宙は Spaceflight API v4)。(2)「選択ソース(最大3・最低1・重複/未知キーを除去)」と「表示中ソース」を別々の購読可能ストアに持ち(localStorage `abops:newsSources`/`abops:newsSource`、既定は従来どおり Qiita+Hacker News)、パネルは選択ソースだけをタブに出し表示中が選択外なら先頭へ丸める(1つだけ選択時はタブを省略)。(3)設定モーダルの「この端末の表示」にチェックボックスのソース選択(チェックした順にタブへ並ぶ・上限到達時は未選択を無効化・最後の1つは外せない)を追加 (2026-07-13)
- ヘッダー右上(設定ボタンの左)に**データの最終更新時刻**(例「最終更新 10:52」)を表示するようにした(ユーザー要望)。各パネル(予定・タスク・メール・天気・ニュース)は TanStack Query で取得しており、成功済みクエリの `dataUpdatedAt` のうち最も新しい時刻を購読して表示する(`useLastUpdated`。`QueryCache.subscribe` + `useSyncExternalStore`)。R キーやポーリング・画面復帰での再取得で値が進むと表示も自動更新される。狭い画面(640px 以下)では中央の日付と同様に非表示 (2026-07-13)
- メール(Gmail)を非表示にしている端末で、これまで空の「Gmail を有効にする」パネルが画面を占めていた枠に、代わりに**ニュースパネル**を表示するようにした(ユーザー要望・TODO #16 のうち A 案)。当初構想の RSS リーダーはブラウザ直 `fetch` だと CORS で弾かれるフィードが多く実装が重いため、**APIキー不要・CORS 対応・JSON 直取り**のソースを叩く方式(天気の Open-Meteo と同じ発想)にした。**Qiita**(日本語の技術記事・新着20件)と **Hacker News**(英語の技術ニュース・トップ20件)をパネル上部のタブで切り替えられる。(1)取得は共通形 `NewsItem` に正規化(`news/api.ts`。Qiita は `qiita.com/api/v2/items` を未認証取得、HN は Firebase API でトップID一覧→各記事を並列取得)。(2)`useNews`(TanStack Query)はニュース表示時のみ取得し、ソースを queryKey に含めるのでタブ切替で自動再取得・10分キャッシュ+15分ポーリング。(3)選択中のソースは端末ローカルに保存(`news/newsSource.ts` の購読可能ストア、localStorage `abops:newsSource`、既定 Qiita)。(4)各記事はタイトル・ポイント(いいね/スコア)・コメント数・投稿者・相対時刻を表示し、クリックで別タブに開く(読むだけの一覧)。(5)`App` は `gmailActive` で Gmail パネルとニュースパネルを同じ枠に出し分け、見出し・スマホのタブ名も「メール」⇔「ニュース」で切り替える。Gmail の再表示は設定(⚙)の「メール(Gmail)を表示」から (2026-07-13)
- 各パネル見出しの右上に、**対応する Google サービスを新規タブで開くボタン(↗)**を追加した(ユーザー要望)。カレンダー(月カレンダー)→Google カレンダー(`calendar.google.com`)、タスク→Google タスク(`tasks.google.com`)、メール→Gmail(`mail.google.com`)。共通の小コンポーネント `PanelLink`(`a target="_blank" rel="noopener noreferrer"`)で、各パネルの見出し行 `.panel__head` に配置。カレンダーを開くボタンは当初「今後の予定」パネルに置いたが、ユーザー要望で「カレンダー」パネル(密度型のみ表示)へ移した (2026-07-13)
- タイトルバー(ヘッダー)の中央に**今日の日付(例「7月13日 (日)」)**を表示するようにした(ユーザー要望)。左右のタイトル・操作ボタンの幅に依らず常に中央へ来るよう絶対配置(`.app__date`、`pointer-events:none` で背後の操作を妨げない)。狭い画面(560px 以下)ではタイトル・設定と干渉しうるため非表示 (2026-07-13)
- PC 向けのキーボードショートカットを追加した(旧 TODO #7)。**R = 全データの更新(再取得)**、**「/」= タスクのクイック追加入力へフォーカス**。入力欄(input/textarea/select・contentEditable)にフォーカス中・修飾キー併用(Ctrl+R 等のブラウザ操作)・モーダル/ボトムシート表示中は無効化する。実装は `App` のグローバル keydown で、更新は `queryClient.invalidateQueries()`、クイック追加へのフォーカスは疎結合シグナル `quickAddFocus.ts`(`requestQuickAddFocus`/`useQuickAddFocusSignal`)経由で `TasksPanel` が入力へ `focus` する (2026-07-13)
- 予定の作成・編集で**複数日にまたがる予定**を扱えるようにした(旧 TODO #10)。ボトムシートの日付欄を「開始日／終了日」の2フィールドに分け、終日・時刻ありのいずれでも複数日スパンを作成・変更できる(開始日を変えると終了日が前にならないよう自動で追従、終了日<開始日は保存時にエラー)。従来は単日前提で、既存の複数日予定はスパンを保持するのみだった (2026-07-13)
- 密度型の24hタイムライン(`TodayTimeline`)で、**空き時間をマウス/ペンでドラッグすると、その時間帯をプリフィルした予定作成シートが開く**ようにした(TODO #17 のうち Phase A。ドラッグ作成の最小単位)。(1)軸(`.timeline__axis`)上の pointerdown〜up の位置を分に変換し15分単位にスナップ、離した時点で `requestCreateEventAt` シグナルを出して `CalendarPanel` の既存の作成シート(`EventSheet`)を時刻プリフィルで開く(新しい作成フロー・ミューテーションは追加せず既存を再利用)。(2)クリック(微小移動)との弁別は押下位置からの**生のピクセル移動量(6px)**で判定し、スナップ境界付近の手ぶれでの誤作成を防ぐ。(3)予定の上で始まったドラッグ・左の時刻ラベル帯(ガター)上・タッチ操作(パネル縦スクロール優先)では作成を開始しない。(4)開始は最大 23:30・終了は開始+15分〜23:45 に正規化し、`type=time` に入らない 24:00 を避ける。(5)ドラッグ中は選択範囲を破線の矩形(`.timeline__drag-sel`)でプレビュー。予定が0件の日でも軸を描画してドラッグ作成・初期スクロールを使えるようにした。上端/下端ドラッグでのリサイズ・本体ドラッグでの移動(Phase B/C)は使用感を見てから判断する残課題(TODO #17) (2026-07-13)
- 密度型の24hタイムライン(`TodayTimeline`)の**予定をクリック(またはキーボードで Enter/Space)すると、既存の編集シートが開く**ようにした(TODO #18)。従来は読み取り専用だった。編集シートとミューテーションは `CalendarPanel` の内部に閉じたまま、`scrollTarget.ts` と同じ購読可能な軽量シグナル(`calendarSheetSignal.ts` の `requestEditEvent`/`useCalendarSheetSignal`、連番 seq をスナップショットに使い同一要求の連続でも発火)で疎結合に伝える方式にした(親/Context への持ち上げは避け、密度型で幅境界をまたいでも状態を失わない既存設計を維持)。編集できるのは書き込み可能(`writable`)かつ確定済み(`pending` でない)予定のみで、それ以外はクリック不可(`role`/`tabIndex` も付けない)。あわせて、シートを開くたびに増える通し番号を `EventSheet` の `key` にし、編集対象が差し替わったときに必ず再マウントさせて「前の予定のフォーム内容が別予定に保存される」不具合を防いだ (2026-07-13)
- ツールの概要をまとめた `README.md` を作成した(ユーザー要望)。目的・解決する課題・主な機能(パネル一覧)・対応デバイス・アーキテクチャ(サーバーレス静的Webアプリ/段階的認可/再ログイン頻度)・セキュリティ方針・開発コマンド・デプロイ・ディレクトリ構成・初回セットアップ・関連ドキュメントを記載。設計合意書 `PLAN.md` のうち今も有効な内容を必要に応じて転記し、役目を終えた `PLAN.md` は削除した(コード/`TODO.md` に残っていた `PLAN.md` への参照も解消) (2026-07-12)
- 密度型(≥1200px)のタスクのバケツで、1列に多数のタスクがあると縦に長くなるため、**10件を超える分を折りたたむ**ようにした(ユーザー要望)。既定では先頭10件だけ表示し、超過分は「他 N 件 ▽」ボタンで展開・「閉じる ▲」で再び畳める。バケツ描画を専用コンポーネント `TaskBucket` に切り出し、各バケツが自分の開閉状態(`showAll`)を持つようにした(`items.slice(0, 10)`。展開トグルは控えめなテキストボタン `.tasks__more`) (2026-07-12)
- 設定モーダルの「この端末の表示」に**配色テーマ(明るさ)**の切り替えを追加した(ユーザー要望)。「端末の設定に合わせる(既定)／ライト(明るい)／ダーク(暗い)」の3択で、OS の設定に関係なくアプリの明暗を固定できる。仕組み: CSS は元々色を `light-dark()` (要素の `color-scheme` を見て明色/暗色を選ぶ関数)で定義しているため、`<html>` の `color-scheme` を JS で上書きするだけで全体の明暗を強制できる('system' は上書きを外して `:root` の OS 追従に戻す)。設定は端末ローカル保存(`displayPrefs.ts` の購読可能ストア `useTheme`/`setTheme`、localStorage `abops:theme`、既定 `system`)。起動時は描画前に `initTheme()` を `main.tsx` から呼んで適用し、初回の明暗ちらつきを防ぐ (2026-07-12)
- 各パネルの読み込み中表示を「読み込み中…」テキストからスケルトン(コンテンツの形をした灰色プレースホルダー)に変更した。予定・タスク・メール一覧は見出し線＋補助線を並べたリスト型(`ListSkeleton`)、今日の24hタイムラインは長さの違う縦バー(`TimelineSkeleton`)、天気は絵文字枠＋数本の線(`WeatherSkeleton`)を表示する(`src/Skeleton.tsx`)。薄い光沢が流れるシマーアニメーション付きで、`prefers-reduced-motion`(動きを減らす設定)では停止する。装飾のため `aria-hidden` で読み上げ対象外にした(月ミニカレンダーはグリッドが即描画されるためスケルトンなし) (2026-07-12)
- メール一覧に既読メールも表示するようにした(ユーザー要望)。従来は受信トレイの未読のみだったのを、「未読を上・その下に既読を新しい順」で並べる。取得は未読(`in:inbox is:unread`・最大20件)と既読(`in:inbox is:read`・最大30件)を別枠で取り、各グループ内を受信時刻の降順にして連結する(`fetchInbox`。件数上限内で未読が既読に押し出されないよう枠を分離)。既読の先頭に区切り見出し「既読」を挿し、既読行は差出人・件名の強調を弱めて未読と区別。開いた行のボタンは未読なら「既読にする」、既読なら「未読にする」を出す(どちらもアーカイブ可)。既読化/未読化は一覧から消さず、一覧内で未読フラグを切り替えて該当セクションへ並べ替える(楽観的更新。アーカイブは従来どおり受信トレイから外れるので一覧から除去)。タブの未読バッジは一覧に既読が混ざるため未読だけを数える方式に変更 (2026-07-12)
- 天気の表示地点を設定モーダルから変更できるようにした(ユーザー要望・旧 TODO #19)。設定の「この端末の表示」に地点欄を追加し、地名で検索(Open-Meteo の Geocoding API=APIキー不要)→候補から選ぶと天気パネルが即その地点で取得し直す。(1)地点は「表示名＋緯度経度」を端末ローカルに保存する購読可能ストア(`weather/location.ts`、localStorage `abops:weatherLocation`、既定=東京。`useSyncExternalStore` でパース結果の参照を保持し無限再描画を防止)。(2)`fetchWeather` は地点を引数で受け取る形に変更、`useWeather` は緯度経度を queryKey に含めるので地点変更で自動再取得。(3)地名検索 `geocodeLocation` は最大5件を日本語表記(例「大阪市（大阪府）, 日本」)で返す。ただし Geocoding API は検索語がローマ字/英語つづり前提(漢字の「大阪」は0件、`Osaka` はヒット)のため、入力欄・注意文はローマ字での検索を案内する。選んだ地点はこの端末だけに保存 (2026-07-12)
- PC 密度型レイアウト⑤(天気)。3列目・上段に今日の天気を小さく表示するようにした(密度型=1200px以上でのみ描画)。(1)Open-Meteo(APIキー不要・無料・CORS対応)から東京の現在気温・天気コードと3日ぶん(今日・明日・明後日)の日別予報(天気コード・最高/最低気温)を取得(`fetchWeather`、Google 認証は不要なので共通の `fetchJson` は使わず素の `fetch`)。地点は定数 `LOCATION`(東京)に固定し、将来ここを設定値へ差し替えられる形にした(グリルの決定「地名固定でv1に入れる」)。(2)天気は WMO 天気コード(数値)で返るため絵文字＋日本語ラベルの対応表(`weatherCodeInfo`)で変換。(3)表示は「大きめの絵文字＋現在気温＋地点/状態/今日の最高(赤)最低(青)」＝今日ぶんを左に置き、その右に明日・明後日のミニ予報(絵文字＋最高/最低)を左罫線で区切って横並び(今日は左の現在表示に含めるためミニ予報からは省く・ユーザー要望)。3列目上段の小枠に収まるコンパクトなレイアウト。(4)天気は頻繁に変わらないため 30分キャッシュ＋30分ポーリング(`useWeather`)。パネルは密度型でのみマウントされるため、そのときだけ取得する。取得失敗・読み込み中の表示も用意。プレースホルダーだった `WeatherPanel` を本実装に置き換え (2026-07-12)
- 月ミニカレンダーの日付をクリックすると、予定パネルがその日の見出しへスクロールするようにした(ユーザー要望)。月カレンダーと予定パネルは別コンポーネントのため、購読可能な軽量シグナル(`scrollTarget.ts` の `requestScrollToDate`/`useScrollToDateSignal`、連番 seq をスナップショットに使い同じ日を連続クリックしても発火)で疎結合に伝える。予定パネルは各日の見出し(`.calendar__day`)に `data-date`(ゼロ埋め YYYY-MM-DD)を付け、シグナル受信時にその要素へ `scrollIntoView`(パネル内スクロール)。その日に予定が無い/表示範囲外の日は何もしない。月セルをボタン化(既定の見た目を消しホバーを付与) (2026-07-12)
- PC 密度型レイアウト③(今日の24hタイムライン)。1列目に、今日の予定を時間軸に配置するタイムラインを実装した(密度型=1200px以上でのみ描画)。(1)既存の7日取得(`useCalendarEvents`)を再利用し(同じ queryKey で重複取得なし)、今日にかかる予定だけを抽出。時刻あり予定は開始/終了で縦に配置、終日予定は上部のチップ、前日/翌日にまたがる予定は 0:00/24:00 にクランプ。(2)表示範囲は活動時間帯(既定 7:00〜23:00)に絞り、範囲外の予定と現在時刻を含むよう時間単位で自動伸縮。(3)現在時刻に赤い横線(丸ポチ付き)を引き、初回に現在時刻が見える位置へパネル内スクロール。(4)同時刻に重なる予定は列(lane)に割り付けて横並び表示(重なりの塊ごとに列数を数える first-fit)。(5)カレンダー色で左罫線・チップの色分け。読み取り専用(予定クリックでの編集は TODO #18) (2026-07-12)
- 月ミニカレンダーの「週の開始曜日」を設定で切り替えられるようにした(日曜始まり=既定 / 月曜始まり)。設定モーダルの「この端末の表示」にプルダウンを追加(ユーザー要望)。端末ローカル設定(`displayPrefs.ts` に購読可能ストア `useWeekStart`/`setWeekStart`、localStorage `abops:weekStart`、既定 0=日曜)。`MonthCalendar` はグリッドの開始位置・曜日見出しの並び・日/土の色分けを週開始に追従させる(列が表す実際の曜日番号 `(weekStart+i)%7` で判定)。曜日見出しの色分けと各日の色分けは列位置ではなく実曜日で決めるため、月曜始まりでも日曜=赤・土曜=青が正しい位置に付く (2026-07-12)
- PC 密度型レイアウト②(月ミニカレンダー)。2列目・上段の右に「今月」の月カレンダーを実装した(密度型=1200px以上でのみ描画)。(1)今月を日曜始まり6週(42セル)のグリッドで表示し、今日を塗りで強調、前後の月の日は薄く表示、日曜=赤・土曜=青で色分け(`MonthCalendar`)。(2)予定のある日に小さなドットを付ける。ドット用データは7日リスト(`fetchUpcomingEvents`)とは別に、グリッド範囲の「予定が1件以上ある日の集合」を返す関数(`fetchEventDaysInRange`)と月クエリ(`useMonthEventDays`、queryKey にグリッドの開始〜終了日を含めてキャッシュ)を追加。取得経路はカレンダー一覧+各カレンダー並列取得で7日リストと共通、複数日・終日にまたがる予定はまたぐ各日にドットが付く。5分ポーリング、予定の作成/編集/削除の実行中はポーリング停止。読み取り専用(v1 はクリック遷移なし) (2026-07-12)
- フェーズ5(設定モーダル): ヘッダー右の歯車ボタンから開く設定モーダルを追加し、4つの区画をまとめた。(1)アカウント: ログイン中の Google アカウント(メールアドレス)を表示し、ログアウト(接続解除)ボタンを置く。メールは追加スコープ無しで済むよう、カレンダーの primary(主)カレンダーの id がメールアドレスそのものである点を利用して取得する(`useAccountEmail` → `calendarList/primary`)。ログアウトはトークンをサーバー側で失効(`revokeToken`=`google.accounts.oauth2.revoke`)させ、ローカルのトークンと同意記録(`grantedScopes`)を破棄して未接続の初期状態=ウェルカム画面へ戻す(`disconnect`/`markDisconnected`/`clearGrantedScopes`。別アカウントへの切替にも使える。誤タップ防止に確認ダイアログを挟む)。(2)この端末の表示: Gmail の表示 ON/OFF トグル(未同意なら union スコープの同意フローを走らせてから有効化)。(3)接続状態: トークン取得時刻の表示と再接続ボタン。(4)アプリ情報: 動いているビルド版(コミットハッシュ)とビルド日時(実機でどの版が動いているか確認でき、PWA 更新遅延 #15 の切り分けにも使える)。あわせて Gmail の端末有効フラグ(`enabled.ts`)を設定モーダルと Gmail パネルの両方から切り替えて即反映できるよう、localStorage 直読みから購読可能な小さなストア(`useSyncExternalStore`)に変更し、`useGmailEnabled` フックを追加した (2026-07-12)
- フェーズ5(タブの件数バッジ): スマホのタブに未読メール件数・期限切れタスク件数を赤いバッジで表示するようにした(レイアウト・バッジのスライス)。タスクタブは期限切れ(`group==='overdue'`)の件数、メールタブは未読件数を表示し、0件のタブには付けない(予定タブはバッジ無し)。件数は各パネルと同じ queryKey のクエリを親(`App`)でも呼んで得る(Fable 助言。TanStack Query の dedupe で取得は重複せず、`select` で件数だけ受けるため件数が変わらない限り再描画されない)。件数フック `useOverdueCount`(タスク)・`useUnreadCount`(メール)を追加。未読は最大20件取得のため20件以上は「20+」と表示する (2026-07-12)
- フェーズ5(既読化・アーカイブ): 未読メールを開いた行に[既読にする][アーカイブ]ボタンを追加し、メールを操作できるようにした。Gmail の既読/未読・受信トレイ/アーカイブはラベルの有無で表されるため、`messages.modify` で UNREAD/INBOX ラベルを付け外しする(`markMessageRead`=UNREAD除去 / `archiveMessage`=INBOX除去、および Undo 用の `markMessageUnread`/`unarchiveMessage`)。一覧は「受信トレイの未読」なので既読化もアーカイブも一覧からは即座に消え、いずれも画面下部の「元に戻す」(5秒)で取り消せる(Undo は逆操作の実行＝未読へ戻す/受信トレイへ戻す。消したメールを受信時刻順で一覧へ戻す)。タスク/カレンダーと同じ楽観的更新(`onMutate` で除去→`onError` で巻き戻し→`onSettled` で invalidate)で実装し、mutation 実行中は5分ポーリングを停止(`useIsMutating`)して楽観結果の上書きを防ぐ。mutation フック `useMarkRead`/`useMarkUnread`/`useArchive`/`useUnarchive` を追加。認証切れ(401)・権限不足(403)の書き込みエラーは `queryClient` の `MutationCache` が一括処理する (2026-07-12)
- フェーズ5(本文プレビュー): Gmail の未読メールをタップすると本文を安全に表示できるようにした。受信メールのHTMLをそのまま描画するとスクリプト実行や外部画像による開封トラッキング(IPリーク)の危険があるため、Fable 助言に基づく多層防御で表示する。(1)本文取得は `messages.get(format=full)` の `payload.parts` を再帰で辿り text/html 優先(無ければ text/plain)で抜き出す(`fetchMessageBody`/`findPart`)。base64url は `atob`→バイト列→`TextDecoder('utf-8')` の2段でデコードし日本語の化けを防ぐ(`decodeBase64Url`)。(2)HTMLは `DOMPurify` でサニタイズ(script/form/style 等を除去、`<a>` に `target="_blank"`+`rel="noopener noreferrer"` を強制、`<img>` の `src` は `data-blocked-src` に退避)(`sanitizeEmailHtml`)。(3)サニタイズ済みHTMLは `<iframe sandbox>`(`allow-scripts` を付けない=中のJSは実行不可)に `srcDoc` で流し込み、ダッシュボード本体からスタイル・実行環境ごと隔離する。iframe の高さは読み込み後に中身の高さへ自動調整。(4)iframe 内に CSP メタタグ(`default-src 'none'; img-src data:` )を埋め、外部画像を既定でブロック(CSS背景画像の追跡も遮断)。「画像を表示」ボタンで `img-src data: https:` に緩めた srcdoc に差し替えて解禁する。(5)プレーンテキストのみのメールは HTML でないため iframe を使わず `<pre>` でそのまま表示(React が自動エスケープ)。本文取得は開いたメールだけ都度行い(`useMessageBody`、10分キャッシュ)、一覧は snippet を隠して本文を展開する。`dompurify` を依存に追加(3.x は型定義を同梱するため `@types/dompurify` は入れない)。インライン画像(`cid:`)・文字コード(ISO-2022-JP 等)・添付ファイルは割り切って未対応とし `TODO.md` #11〜#13 に記録 (2026-07-12)
- フェーズ5(最初のスライス): Gmail 受信トレイの未読一覧表示を実装。(1)3つ目のパネル「メール(受信トレイ・未読)」を追加(`GmailPanel`)。(2)Gmail はこの端末で明示的に有効化したときだけ動く方式にした。未有効なら「Gmail を有効にする」ボタンを表示し、押すと union スコープ(既存の calendar/tasks + `gmail.modify`)をまとめて1本のトークンで要求する(段階的認可。Gmail は初回同意対象外のため実機で初めて同意ダイアログが出る)。「この端末で表示するか」は Google アカウント単位の同意(`grantedScopes`)とは別に端末ローカル設定(localStorage `abops:gmailEnabled`)で持ち、会社PC等で「アカウントは同意済みだがこの端末では非表示」を表現できる(`enabled.ts`、`isGmailEnabled`/`setGmailEnabled`、「この端末で非表示」ボタン)。(3)有効時は受信トレイの未読(`in:inbox is:unread`)を最大20件、差出人名・件名・日時・スニペット(抜粋)で新着順に一覧表示する。取得は `messages.list` で id を集め、各 id を `messages.get`(`format=metadata`、From/Subject/Date ヘッダのみ)で並列取得する N+1 方式(`fetchInboxUnread`/`fetchMessageIds`/`fetchMessageMeta`)。スニペットは HTML エンティティ(`&amp;` 等)でエスケープされて返るため `DOMParser` で復号(`decodeHtmlEntities`)、`internalDate`(ミリ秒エポック文字列)から日時を算出。本文表示・既読化・アーカイブは次スライス。`useGmail` フックは接続済み・要再接続でない・この端末で有効・`gmail.modify` 同意済みの全条件が揃ったときのみ 5分間隔で取得 (2026-07-12)
- フェーズ4-c: カレンダー予定の「作成・編集・削除」を実装。(1)予定パネル上部の「＋予定」ボタンでボトムシートを開き、タイトル・作成先カレンダー(owner/writer のみ選択肢に、既定は primary)・終日トグル・日付・開始/終了時刻を指定して作成。時刻入力は `<input type=date>`+`<input type=time>`×2、終了既定は開始+1時間。(2)予定をタップすると下に[編集][削除]を展開(書き込み可能なカレンダーの予定のみ。読み取り専用カレンダーの予定は操作不可)。(3)編集は同じボトムシートでタイトル・日時を変更し1回の PATCH で保存(既存の複数日スパンは保持)。(4)削除は即削除し「元に戻す」を5秒表示。Undo は再作成ではなく同じ event id に `status:"confirmed"` を PATCH して復元(IDが変わらず、繰り返し予定の1回にも同じ方法が効く)。(5)繰り返し予定はインスタンス(この回)を対象に編集・削除し、スナックバー等に「この回のみ」と明示(シリーズ全体の編集は純正へ)。書き込みAPI `createEvent`/`updateEvent`/`deleteEvent`/`restoreEvent`、書き込み可能カレンダー取得 `fetchWritableCalendars`、mutation フック(`useCreateEvent`/`useUpdateEvent`/`useDeleteEvent`/`useRestoreEvent`)を追加。タイムゾーンはオフセット無しのローカル日時+`timeZone`(端末TZ)で送信、終日 end.date は排他的(+1日)、patch での終日⇔時刻あり切替は不要側フィールドを `null` 明示、削除済み(404/410)は冪等化。すべて楽観的更新で即時反映し、7日ウィンドウ外に作った予定はその旨をトースト表示。mutation 中はポーリング抑制 (2026-07-12)
- フェーズ4-b: タスクの「明日へ/来週へ・編集・削除」を実装。(1)各タスクの行をタップすると下にアクションバー(明日へ/来週へ/編集/削除)を展開(開くのは常に1行だけ)。(2)「明日へ」=今日+1日、「来週へ」=今日基準の翌週月曜に期限を変更(期限切れタスクでも過去日にならないよう現在の期限ではなく今日を基準にする)。移動も画面下部の「元に戻す」で取り消せる。(3)「編集」は下から出るボトムシート(小モーダル)でタイトルと期限(なし/今日/明日/日付指定=OS標準の日付ピッカー)を変更し1回のPATCHで保存。(4)「削除」は即削除し「元に戻す」を表示(Google Tasks に復元APIが無いため、Undoは同じ内容での再作成で実現。削除が既に完了している404は成功扱いにして冪等化)。書き込みAPI `updateTask`(タイトル・期限、期限クリアは `due:null`)/`deleteTask` と mutation フック `useUpdateTask`/`useDeleteTask`/`useInsertTask`(追加と削除Undoの復元を共用)を追加。グループ分け(期限切れ/今日/今後/期限なし)を保存値ではなく毎回 `due` から導出する方式に変更し、期限変更時に行が正しいグループへ即移動するようにした。すべて楽観的更新(onMutate→onError巻戻し→onSettled invalidate)で即時反映 (2026-07-12)
- フェーズ4-a: タスクの「完了」と「クイック追加」(書き込みの最初の機能)を実装。(1)各タスクに丸いチェックを付け、押すと即完了して一覧から消える。誤タップ対策に画面下部へ「元に戻す」スナックバー(通知バー)を5秒表示し、押すと未完了に戻す(Undo=Google Tasks の再オープン操作。完了は即サーバー確定し、リロードやタブを閉じても操作が飛ばない方式を採用)。(2)パネル上部の入力欄でデフォルトリストにタスクを追加。「今日」ボタンで期限を今日にワンタップ指定できる(既定は期限なし)。書き込み系API関数 `completeTask`/`reopenTask`/`insertTask` と mutation フック(`useCompleteTask`/`useReopenTask`/`useAddTask`)を追加。すべて楽観的更新(onMutate でキャッシュを即書換→onError で巻戻し→onSettled で invalidate=再取得)で即時反映し、最終的にサーバーと整合させる。自分の操作中(mutation 実行中)は5分ポーリングを止め、`onMutate` の `cancelQueries` と併せて「アプリが勝手にリストを書き換える」現象を防止。トークン失効時の書き込みも再接続UXに流すため `MutationCache` にも 401 ハンドラを追加(`QueryCache.onError` は書き込みエラーを拾わないため)。期限の書戻しは `toISOString()` を経由せずローカル日付文字列から `T00:00:00.000Z` を組み、深夜帯の1日ズレを回避 (2026-07-12)
- フェーズ3: Google Tasks の読み取り表示を実装。全リストの未完了タスクをリストごとに並列取得し(`fetchAllTasks`)、「⚠期限切れ(赤)/今日/今後/期限なし」の4グループに分類して表示する(`TasksPanel`)。期限 `due` は日付のみ・UTC0時で返るため、`new Date()` に変換せず先頭10文字(YYYY-MM-DD)を文字列比較して分類し、タイムゾーン起因の1日ズレを回避。各タスクにリスト名の小ラベルを表示。カレンダーと同じ構成(feature フォルダ+`fetchJson`+TanStack Query、5分ポーリング、401共通ハンドラ)。初回ログインのスコープに Tasks を追加(`INITIAL_SCOPES`、段階的認可)。画面はカレンダーとタスクの2パネルを広い画面で2カラム・狭い画面で1カラム表示にした (2026-07-12)
- `vite-plugin-pwa` を導入し、最小の Service Worker を追加した。Android で `chrome://webapks` にアプリが登録されず「ホーム画面に追加(ショートカット)」しか出ずスタンドアロン起動にならなかった原因が、Chrome の PWA インストール判定に Service Worker が事実上必要だったことと判明したため(PLAN の TODO #6「初期はSW入れない」を見直し)。SW はアプリシェル(HTML/JS/CSS/アイコン)のプリキャッシュのみで、Google API/GISスクリプト(別オリジン)は一切キャッシュせず常にネットワーク直行。マニフェストはプラグインに一本化し(手書きの `public/manifest.webmanifest` を削除)、start_url/scope は base(`/ABOperations/`)から自動設定 (2026-07-12)
- 起動時のサイレント再認証を実装。以前この端末でカレンダーに同意済み(`grantedScopes` に記録)なら、起動時に `requestAccessToken({ prompt: '' })` でポップアップ無し・クリック無しの自動ログインを試みる(`requestTokenSilent`/`trySilentConnect`)。成功すれば毎回のログイン操作が不要になる。失敗(セッション切れ・未同意・タイムアウト8秒)時は静かに通常のウェルカム画面へフォールバックする。トークンは従来どおりメモリ保持のみで永続化しない(セキュリティ方針は不変、UXのみ改善)。試行中は「接続中…」表示でログインボタンの点滅を防ぐ (2026-07-12)
- PWA マニフェスト(`public/manifest.webmanifest`)とアイコン(192/512/マスカブル512/apple-touch-icon 180)を追加し、Android の「ホーム画面に追加」でスタンドアロン起動(ブラウザのバー無しのアプリ風表示)できるようにした。iPhone 用の `apple-touch-icon`・`apple-mobile-web-app` メタも設定。start_url/scope/アイコンsrc は base path 配下で解決されるよう相対パスにした。Service Worker(オフライン対応)は初期スコープ外のまま (2026-07-12)
- フェーズ2(GISログイン+カレンダー読み取り)の実装。(1)認証をモジュールシングルトン(`tokenStore`=生トークンをメモリ保持/`gisClient`=GISトークンクライアントのPromiseラッパー)＋薄い外部ストア(`authStore`=isConnected/needsReconnect/grantedScopes/acquiredAt を useSyncExternalStore で購読)の二層構成で実装。(2)データ取得に TanStack Query を導入し、`QueryCache` のグローバル `onError` で 401(`AuthError`)を捕捉して自動的に再接続UXへ切り替え(該当クエリは `enabled` で自動停止、401はリトライしない)。(3)カレンダーは全カレンダーを並列取得し今日から7日分を日付ごとに時系列表示(`fetchUpcomingEvents`/`CalendarPanel`)。5分ポーリング+画面復帰時更新に対応。(4)未ログイン/ログイン/セッション切れ(再接続バナー1本)の画面状態と、再ログイン頻度の実機検証用にトークン取得時刻の表示を実装。段階的認可のためスコープを機能別にグループ化し初回はカレンダーのみ要求 (2026-07-12)
- フェーズ1(公開フロー確立)完了。React + Vite + TypeScript の最小プロジェクトを scaffold し、GitHub Pages 自動デプロイ用の GitHub Actions ワークフロー(`.github/workflows/deploy.yml`)を作成。GitHub 公開リポジトリ `sabe1991/ABOperations` を作成し、Pages のソースを「GitHub Actions」に設定してデプロイ成功を確認した(公開URL: https://sabe1991.github.io/ABOperations/ が HTTP 200 で表示、JS/CSS/画像アセットが base path `/ABOperations/` 配下で解決、バンドルにコミットハッシュが埋め込まれデプロイ反映を目視確認できることを検証済み)。暫定画面(`src/App.tsx`)はアプリ名・アセット画像・ビルド情報(コミットハッシュ/ビルド日時)を表示する (2026-07-12)
- プロジェクト開始。グリルセッションでダッシュボードの設計を合意し、`PLAN.md`(設計合意書)、`TODO.md`、`STATE.md` を作成した (2026-07-12)

## Changed

- **朝刊リデザインの第2段階として、パネルを「カード」から新聞の「欄(段組み)」へ作り替えた**(`redesign-chokan` ブランチ)。各パネルの面の背景・角丸・やわらかい影・四辺の枠をすべて廃し、**上に太い墨罫(柱罫。`border-top: 2px solid var(--rule-strong)`)**を載せたフラットな欄にした。欄どうしの区切りは、ダッシュボードのグリッド(`.panels`)の**隙間を 1px にして下地に罫色(`--border`)を敷く**手法で、隙間が連続した**ヘアライン罫(段組みの縦罫・横罫)**として見えるようにした(各パネルは地色で塗るので境目だけが細い罫として残る)。あわせて**見出し(`.panel__title`)の左にあった暖色のグラデ縦バーを撤去**し(`::before` を削除)、新聞の欄見出しらしく明朝の字だけで立てるようにした(頭の区切りは上の柱罫が担う)。これによりログイン後の画面が「カードが浮かぶダッシュボード」から「罫線で仕切られた紙面」へと見た目が大きく変わる(第1段階の配色・書体の変更だけでは差が分かりにくかったため) (2026-07-24)
- **アプリのアイコン(ブラウザのタブ favicon・ホーム画面/PWA アイコン・iOS のアイコン)を朝刊デザインに合わせて刷新した**(ユーザー要望)。従来は「青地に白のゴシック AB」で新デザインと不整合だったため、**生成りの紙色(#faf9f5)地に明朝の「AB」＋朱(#c0342b)の二重罫**(新聞のマストヘッド調)へ作り替えた。ベクター原本(SVG)を macOS の `sips` で各サイズの PNG に書き出し、`public/apple-touch-icon.png`(180)・`public/icons/icon-192.png`・`icon-512.png`・`icon-maskable-512.png`(Android の丸抜きに備え内容を80%に縮小して安全域へ収めたもの)を差し替え。あわせて**ブラウザのタブに出る favicon を新設**し(従来は `rel="icon"` の指定自体が無かった)、`public/favicon.svg`(ベクター・主)＋`icons/icon-192.png`(PNG フォールバック)を `index.html` の `<link rel="icon">` に登録、SW のプリキャッシュ(`includeAssets`)にも `favicon.svg` を加えてオフラインでも出るようにした (2026-07-24)
- **朝刊(新聞紙面)リデザインの第1段階として、デザイントークン(配色・書体・角丸・影)を全面的に差し替えた**(`redesign-chokan` ブランチ)。トークン名と `light-dark()`(ライト値とダーク値を1関数で持つ CSS 機能)の構造はそのままに、値だけを新聞紙面調へ変更したので、これだけで全体の色味・書体が一斉に切り替わる(レイアウトや部品構造は次段階以降)。具体的には、(1)`--font-display`(見出し・題字用書体)を丸ゴシックから**明朝体**(`Hiragino Mincho ProN`→游明朝→`Noto Serif JP`→`serif`)へ、(2)地・面・文字・アクセントを**生成りの紙色×墨×朱**へ(`--bg`/`--surface` は同色 `#faf9f5`(暗 `#161513`)にして紙面のフラットさを出し、`--text` を墨 `#1c1a16`(暗 `#eceae4`)、アクセント `--primary` を新聞見出しの朱 `#c0342b`(暗 `#e26a54`)に。白文字ボタン用 `--primary-btn` は白文字 AA を明暗とも満たす濃い朱 `#a52a22` に分離)、(3)`--radius`/`--radius-lg` を 12/18px から **2px**(角をほぼ立てる)に、`--shadow-sm` を `none`・`--shadow-md` を前面モーダル専用の淡い影だけに、(4)太い墨罫(パネル上の柱罫・題字下の二重罫)専用トークン **`--rule-strong`** を新設(ライトは本文色、ダークは"檻"のような圧迫を避けるため一段くすませた墨 `#837c6e`)。あわせて OS のステータスバー色(`index.html` の `theme-color`・PWA マニフェストの `theme_color`/`background_color`・`displayPrefs.ts` の手動テーマ上書き定数)も新しい紙色 `#faf9f5`/`#161513` に更新した。主要な補助テキスト・アクセントは地の上で WCAG AA(4.5:1)以上を維持(実測 muted 約6.2:1・朱の文字 約5.5:1/暗4.6:1) (2026-07-24)
- `README.md` の記載を**現状のコードに合わせて更新**した(実装が先行して記載が追いついていなかったため)。(1)機能一覧に**ニュースパネル**の行を追加(メールをオフにした端末で代わりに出る枠であること・全7ソースから最大3つを設定で選ぶこと)。従来まったく記載が無く、README だけ読むと存在に気付けなかった。(2)メールの行に、後から追加した**未読に戻す・スター・新規作成／返信の送信・添付ファイルのダウンロード・埋め込み画像**を追記(従来は「本文プレビュー・既読化・アーカイブ」しか書いておらず実態より狭かった)。(3)スマホの下端タブ(バッジ付き)・設定モーダルで変えられる項目の一覧・**キーボードショートカット表**(`R`/`/`/`Esc`)を追加。(4)**セキュリティ方針の記載が実装と食い違っていたのを修正**した。2026-07-15 に外部画像を既定表示へ変えた際に README を直しておらず、「リモート画像を既定でブロック」という**逆の説明のまま**だった(セキュリティ上の既定値の説明が実物と逆なのは危険なため、設定で切り替えること・既定はON・OFF時は `img-src` を `data:` に絞ること・`no-referrer` によるハードニングを明記し、`cid:` 埋め込み画像は設定に関わらず常時表示であることを分けて書いた)。あわせて CSP が `default-src 'none'` で通信・スクリプトを全遮断している点も追記。(5)アーキテクチャ表に「外部API(天気・ニュース)は**APIキー不要・CORS 対応・JSON 直取りの公開APIだけ**を使う」方針を明記(ソース追加時の判断基準になるため)。(6)ディレクトリ構成に、未記載だった `news/`・`config.ts`・`ErrorBoundary.tsx`・`PanelLink.tsx`・`errorMessage.ts`・`fetchTimeout.ts`・`pwaUpdate.ts`・`roving.ts`・`tempId.ts`・`useDialog.ts`・`useLastUpdated.ts`・`google/pool.ts` を追加 (2026-07-16)
- ニュースの **Hacker News の取得を1リクエストに削減**した。従来は公式の Firebase API がトップ記事のID一覧しか返さないため、ID一覧の取得(1回)＋各記事の詳細取得(20回)で毎回**計21リクエスト**を投げ、部分失敗に備えて並列数の制御(`mapPool`)と全件失敗判定(`throwIfAllRejected`)も必要だった。同じ内容(タイトル・URL・作者・ポイント・コメント数・時刻)を1回で返す **HN Algolia の検索API**(`hn.algolia.com/api/v1/search?tags=front_page`・鍵なし・CORS 対応)へ切り替え、他ソースと同じ「1回叩いて整形するだけ」の形に揃えた(表示内容は従来と同じ)。あわせて各ソースに散っていた取得件数 20 のリテラルを定数 `LIMIT` にまとめ、投稿者が自由入力するソース(dev.to 等)でタイトルに改行が混ざると2行クランプの表示が崩れるため `normalizeTitle` で連続空白を1つに潰すようにした (2026-07-15)
- ニュースソースの追加・変更に伴い `index.html` の **CSP(`connect-src`)の許可ホストを更新**した。追加: `hn.algolia.com`・`dev.to`・`api.github.com`。削除: `hacker-news.firebaseio.com`(HN の取得先変更で不要になったため)。CSP は通信先を許可リストで絞る仕組みのため、ここに足し忘れると新ソースの取得がブラウザでブロックされる(実ブラウザで新3ホストが通り、削除した旧ホストがブロックされることを確認済み) (2026-07-15)
- メール本文の**外部画像を「本文先→画像は順次」読み込みに変え、読み込み中バッジを表示**するようにした(ユーザー要望)。従来は画像表示ONだと srcDoc 組み立て時に全画像の `src` を一括復元して一斉(並列)ロードしていた。これを、srcDoc では画像を `src` 空のまま流して本文テキストを先に表示し、iframe ロード後に親(`HtmlBody`)が `data-blocked-src` を **同時3枚まで・1枚ずつ順に** 復元する方式にした(`startSequentialImageLoad`)。本文枠の右上に「画像を読み込み中… 3/10」の進捗バッジ(`.gmail__img-loading`)を出し、全部終わると消す。iframe には `allow-scripts` を付けない方針(本文JSを実行させない多層防御)なので中では制御できないが、`allow-same-origin` により親から `contentDocument` の img を操作でき、親が src をセットしてもロードは iframe の CSP に従う(https 許可済み)ため成立する。厳密な1枚ずつ直列は「応答しないトラッキング画像1枚で後続が全部詰まる」ため避け、同時3枚＋各画像10秒タイムアウトにした(Fable 助言)。メール切替・画像OFF・アンマウント時は世代トークンで進行中のループを打ち切る。埋め込み画像(`cid:`→`data:`)は外部通信を伴わず退避対象外なので、この経路とは無関係に即表示される (2026-07-15)
- PWA 更新トーストの「更新して再読み込み」ボタンに**押下フィードバック**を付けた(ユーザー指摘: 押しても色が変わらず、押せたか分からない)。(1)押した瞬間にボタン文言を**「更新中…」**へ変え無効化(`applying` state・`is-applying` クラス)して押下が確かに伝わるようにした(新 Service Worker の有効化〜リロードには一拍あり、その間に無反応に見えていた)。(2)ホバー/押下(`:hover`/`:active`)で背景と文字色を反転させ、押せることが分かるようにした (2026-07-15)
- ニュースの記事に出る**「◯分前」の基準を分かりやすくした**(ユーザー要望・現在時刻からなのか最終更新時刻からなのか分からない、という指摘)。(1)1分ごとに再計算する仕組み(`NewsPanel` の分刻みタイマー)を入れ、放置しても常に**現在時刻からの経過**を示すようにした(従来は再描画時にしか更新されなかった)。(2)相対時刻にマウスを乗せると正確な投稿日時と「現在時刻からの経過を表示」の補足が出る `title` を付けた(`formatAbsolute`)。※`title` はホバー専用のためタッチ端末では表示されない (2026-07-15)
- タスク編集シートの期限チップ「なし/今日/明日/来週」を、**押した瞬間に保存して閉じる（即確定）**ようにした(ユーザー要望)。1タップで期限が一意に定まるこの4つは保存ボタン不要にし、日付選択の手間がある「日付指定」だけ従来どおり保存ボタンで確定する。実装は `EditSheet` の `handleChip` で、押したモードから直接期限を算出(`dueForMode`。`setDueMode` 直後は state が古い値のままで `resolveDue` 経由だと1つ前のモードで確定してしまうため関数化した)、その時点のタイトルも一緒に保存(空なら元タイトルにフォールバック)し、タイトルも期限も無変更なら通信せず閉じるだけにした。ドラッグでの期限変更と同様、押し間違えても戻せるよう「元に戻す」スナックバー付きで確定する(`onQuickSave`→`handleQuickSaveEdit`) (2026-07-15)
- 未読メールを**開いた瞬間に既読化**し、本文モーダルの「既読にする」ボタンを廃止した(ユーザー要望・Gmail 本家と同じ挙動)。行タップのハンドラ(`handleOpen`)で未読なら `markRead` を発火(成功時は通知を出さない)、モーダルの操作列は条件分岐をやめて常に「未読にする」だけ出す(読まなかったことにしたいとき用)。「既読にする」を廃したことで既読化の失敗(通信断など)が無音になり未読のまま復旧手段が UI から消えるのを避けるため、失敗時だけスナックバーで知らせる(もう一度開けば再試行される) (2026-07-15)
- 月ミニカレンダーを**矢印キーで日移動したとき、タイムラインだけでなく「今後の予定」リストもその日へ連動スクロール**するようにした(ユーザー要望。従来は表示日の切替のみでスクロールしなかった)。クリック時と同じく `requestScrollToDate` を呼ぶが、キー長押しのリピート中に毎回 `scrollIntoView({behavior:'smooth'})` すると震えて進まなくなるため、スクロールは原則キーを離した時(`onKeyUp`)に最後の1回だけ実行し、単押し(リピートでない最初の押下)のみ即スクロールして即応性を保つ(`pendingScrollRef`)。その日に予定が無い(見出しが無い)ときはスクロールしない (2026-07-15)
- HTMLメール本文が**横にはみ出して横スクロールが必要になる問題**を修正した(ユーザー要望)。固定幅(600px 等)で作られた HTML メールは、本文 iframe の自然な横幅がモーダル枠を超えると、iframe をその自然幅まで広げてから `transform: scale()` で全体を縮小し、**横スクロールなしで枠内に収める**ようにした(スマホで非対応メールを見たときのズームアウト表示と同じ挙動)。レスポンシブなメールは等倍のまま表示する。縮小時は占有領域を枠(`.gmail__body` に `overflow:hidden`)で確保し、高さは縮小後の値に合わせる。端末回転・ウィンドウ幅変更時は縮小率を測り直す(高さ追従の `ResizeObserver` は幅を変えず＝リフロー無限ループを避ける) (2026-07-14)
- メール本文モーダルで、**件名と×ボタンを上部に固定**し、本文だけがスクロールするようにした(ユーザー要望・長い本文でも件名・閉じるが常に見えるように)。モーダルを縦フレックスにして見出し(`.gmail-modal__header`)を固定、その下(`.gmail-modal__scroll`)だけを `overflow-y:auto` にした。あわせて、**「画像を表示」ボタンを本文上の単独行から操作ボタン列(返信・既読/未読・アーカイブ)へ移動**した(単独で行が変わって浮いて見えるのが気になる、というユーザー指摘)。表示状態はモーダル側で持ち、本文(`HtmlBody`)がブロック画像の有無を親へ通知して操作列に出し分ける方式にした (2026-07-14)
- メールをタップしたときの本文表示を、**パネル内展開から画面中央のモーダルに変更**した(ユーザー要望・狭いパネル内では読み書きがしづらいため)。行タップで本文モーダル(`MessageModal`)を開き、件名・差出人・受信日時のヘッダと操作(返信/既読・未読/アーカイブ)、本文・添付を広い領域(`.gmail-modal` 最大40rem・`max-height:85vh` でスクロール)に表示する。既読/未読/アーカイブは実行後モーダルを閉じ、返信はモーダルを閉じてから作成シートを開く。`useDialog` で Esc・フォーカストラップ・スクロールロック・フォーカス復帰を付与。行はインライン開閉をやめ、スニペットを常時表示するだけの単純な行にした(本文取得・サニタイズ・iframe 隔離表示・添付の仕組みは従来のまま流用) (2026-07-14)
- PWA の更新トーストを分かりやすくした。(1)ボタン文言を「再読み込み」→**「更新して再読み込み」**に変更(通常のブラウザ再読み込み(F5)では待機中の新 Service Worker は有効化されず、このボタンだけが更新を確定できる。「再読み込み」だと F5 と同じに見えて、押さずに放置され『更新トーストが出続ける』混乱を招いていたため)。(2)**「×(閉じる)」ボタンを追加**し、今すぐ更新しないときもトーストを閉じられるようにした(`dismissUpdate`。待機中の新版は残り、次回起動や「更新して再読み込み」で適用できる) (2026-07-13)
- パネル見出しの**「Gmail を開く」ボタンを、スマホでは Gmail アプリで開く**ようにした(ユーザー要望)。従来は常に web 版(`https://mail.google.com/`)を新規タブで開いていた。環境別にリンク先を出し分けるヘルパ `gmailLink()` を追加し、Android は `intent://…;package=com.google.android.gm;S.browser_fallback_url=…;end`(アプリ未導入なら web へ自動フォールバック)、iOS は URL スキーム `googlegmail://`、PC は従来どおり web 版(新規タブ)にした。アプリ起動 URL では空白タブが残らないよう `PanelLink` に `newTab` を追加し、モバイル時は `target="_blank"`/`rel` を付けない (2026-07-13)
- **#9** タスクの**編集シートに「タスクは日付単位です(時刻は設定できません)。時刻が必要な予定はカレンダーへ」の注記**(`.sheet__hint`)を添えた。Google Tasks の公開APIは期限が日付のみで時刻を持てないため、時刻付きで管理したい項目はカレンダー予定へ、と使い分けを案内する (2026-07-13)
- メールを**開いて既読/未読を切り替えたら、その本文を自動で閉じる**ようにした(ユーザー要望)。従来は既読化しても本文が開いたまま既読セクションへ移動し、視線が飛んで分かりにくかった。既読化・未読化の操作時に `setOpen(false)` で閉じてから状態を更新する(あわせて #45 で Gmail のミューテーション＝サーバー更新処理を行から親パネルへ集約し、アーカイブで行が消えても Undo が安定して動くようにした) (2026-07-13)
- 予定の編集ボトムシートの**日時入力を2列レイアウト**に変更した(ユーザー要望)。上段[開始日｜終了日]・下段[開始時刻｜終了時刻]の格子にし、終了日の真下に終了時刻が来るようにして開始/終了の対応を目で追いやすくした(終日予定のときは時刻行を隠す) (2026-07-13)
- **スマホで予定・タスクの編集シートを開いたとき、タイトル入力へ自動フォーカスしない**ようにした(ユーザー要望)。ソフトキーボードがせり上がって画面が隠れるのを防ぐため、`(pointer:coarse)`(タッチ端末)では初期フォーカスをタイトルではなくシート枠へ寄せる(PC では従来どおり先頭入力へ) (2026-07-13)
- **#28** 取得失敗時の表示を、生の技術的エラー(英語の例外文字列)から**平易な日本語の定型文**に変えた。共通の `toUserMessage`(`errorMessage.ts`)で、認証切れ・権限不足・タイムアウト・ネットワーク断・サーバー混雑(429/5xx)などを「再接続をお試しください」等へ写像し、`CalendarPanel`/`TasksPanel`/`GmailPanel`/`ErrorBoundary` の表示に適用した (2026-07-13)
- **#56/#64/#66/#44/#33/#62** 堅牢性まわりを整えた。(1)#56 外部 fetch(天気・ニュース・ジオコーディング)に**タイムアウト/Abort**(`fetchWithTimeout`、既定8秒)と**実行時の型ガード**(`asArray`/`asObject`)を入れ、無応答での固まり・想定外レスポンスでの白画面を防ぐ。(2)#64 `fetchJson` が 204/空ボディで `undefined` を返すよう本文有無で分岐(`undefined as T` の型偽装を解消)。(3)#66 `useLastUpdated` を全走査から `updated`/`removed` 系イベントのみで再計算する差分方式へ。(4)#44 楽観的追加の仮 ID を `temp-${Date.now()}` から `crypto.randomUUID()` ベース(`tempId`)にし同一ミリ秒の衝突を回避。(5)#33 書き込み中(`isMutating>0`)は `refetchOnWindowFocus` を止め、楽観的更新のちらつき(一瞬の巻き戻り)を防ぐ。(6)#62 同意済みスコープの記録を、`addGrantedScopes`(和集合で積むだけ)に加え**トークン応答の `response.scope` で丸ごと上書きする** `setGrantedScopes` を追加し、ユーザーが Google 側で権限を取り消したときも記録が実態に追従して失効を反映できるようにした(本アプリは常に「欲しい権限の全集合」を要求するため上書きが安全) (2026-07-13)
- **#26/#58/#59/#61/#57/#68/#39** 見た目・使い勝手の細かな改善を行った。(1)#26 Undo スナックバーがスマホの下端タブバーと重なる問題を、タブバー高さ+セーフエリアぶん持ち上げて回避。(2)#58 Gmail 表示 ON のときは不要なニュースソース選択を設定から畳む。(3)#59 タイムラインの予定タイトルを `--fs-2xs`→`--fs-xs` に拡大。(4)#61 タイムラインの予定に `grab` カーソルとグリップを付けドラッグ操作を可視化。(5)#57 色・記号のみで伝えていた箇所(ニュースの `▲`/`💬`、天気の最高/最低)に `aria-label` を付与し記号を `aria-hidden` 化。(6)#68 シート/モーダル/トーストにフェード・スライドの開閉トランジション(約150ms、`prefers-reduced-motion` で無効化)。(7)#39 月ミニカレンダーの「読み込み中…」テキストを撤去し `aria-busy` に統一 (2026-07-13)
- **#35/#42/#43**(ドキュメント整備) README のセキュリティ方針を実装に合わせて修正・拡充した。(1)#35 アクセストークンの保存を「メモリ保持のみ」→「`sessionStorage` 保存(同タブのみ・リロードで維持・タブを閉じると消える)」に修正し、XSS で読まれ得るトレードオフを明記(実装は `sessionStorage`)。(2)#11 の cid 埋め込み画像を `data:` で既定表示する旨をセキュリティ節に追記。(3)#42「破壊操作の確認方針(Undo 可能＝無確認/不可逆＝確認ダイアログ)」の節を新設。(4)#43 メール本文 iframe の `sandbox` 直上に「`allow-scripts` を絶対に追加しない」旨の警告コメントを整備(`allow-same-origin` は高さ計測・Android リンク横取りのため必要で、`postMessage` 化による除去は #14 の Android リンク処理に依存するため見送り) (2026-07-13)
- 設定モーダルの「表示」タブで、**「これらの設定はこの端末だけに保存されます。」の注記をタブの一番上へ移動**し、括弧書き(「会社PCなど端末ごとに切り替えられます」)を削除した(ユーザー要望)。端末ローカル保存であることを最初に伝え、文言を簡潔にした (2026-07-13)
- **タスクの行をタップしたときの挙動を、下にアクションバー(明日へ/来週へ/編集/削除)を展開する方式から、予定と同じく編集ボトムシートを直接開く方式に変更**した(ユーザー要望)。編集シート内で期限(なし/今日/明日/来週/日付指定)を変更でき、削除ボタンもシート左端(危険色・予定シートと同じ配置)へ移した。完了は従来どおり行左の丸チェックでワンタップのまま。旧アクションバーの展開 state・`RESCHEDULE_BY_BUCKET` 等は撤去し、「来週へ」は編集シートの「来週」チップで1タップ相当を維持 (2026-07-13)
- **#53** メール本文リンクを Chrome 本体で開く `toIntentUrl` に、intent 構文を壊しうる文字(`#`・空白・制御文字 U+0000–U+001F)が data 部に混ざった場合は intent 化せず**通常遷移に委ねる防御ガード**を追加した(正当な URL は percent-encode 済み・フラグメントは別管理のため弾かれない。null 時は本体側で既定動作＝通常の新規タブ表示)。あわせて本文 iframe の `sandbox` 各トークンの意図(`allow-popups-to-escape-sandbox` は外すと外部リンク先が sandbox を継承してスクリプト無効で壊れるため**必要**)と「`allow-scripts` を絶対に追加しない」旨の警告コメントを明記した(レビュー指摘の「このトークンは不要」は誤りと確認し維持) (2026-07-13)
- 3カラム表示へ切り替わる画面幅の境界を **960px → 800px** に下げた(ユーザー要望)。従来は 960px 未満でスマホ用のタブ表示(1枚ずつ)に落ちており、まだ横に余裕があってもタブになってしまっていた。境界を 800px まで下げ、狭い画面のときだけタブ表示にする(当初 768px にしたが少し狭かったため 800px に微調整)。あわせて中間幅の3カラムがはみ出さないよう `.panel` に `min-width: 0` を追加(メール本文 iframe など幅の広い中身が列幅まで縮むように)。`index.css` の該当する `@media`(スマホ非表示・広い画面の画面高固定・中間幅3カラム・タブバー非表示)の境界をすべて 799/800 に統一。密度型(段積み)の 1340px 境界は据え置き (2026-07-13)
- 設定モーダルを**タブ形式**に整理した(ユーザー要望。項目が増えて縦に長くなっていたため)。「**表示**(この端末の表示: テーマ・Gmail 表示・出典名・週の開始曜日・天気の地点)」「**アカウント**(ログイン中のメール・ログアウト / 接続状態・再接続)」「**情報**(アプリのビルド版)」の3タブに分け、既定は「表示」を開く。実装は `SettingsModal` に `activeTab` state と `.settings__tabs`/`.settings__tab`(下線でアクティブ表示)を追加し、各区画をタブで出し分ける。タブ直下の区画は上罫線を消してタブの下線と二重にならないようにした。あわせて「この端末の表示」見出しはタブ名と重複するため省き、Gmail トグルの説明を「OFF にするとニュースを表示」に更新 (2026-07-13)
- 「今後の予定」で**予定を選ぶと、その場で詳細/編集シートを開く**ようにした(ユーザー要望)。従来は予定をタップすると下に[編集][削除]ボタンが開くだけだったのを、タップで直接シートを開き、場所・メモ欄まで一目で見られるようにした。書き込み可能な予定は編集でき(シート下部に削除ボタンを移設)、読み取り専用の予定は入力を無効化した「予定の詳細」表示(場所・メモも閲覧可・保存/削除なし)にした。行の展開UI(`expandedId`)と一覧内の[編集][削除]は廃止(`CalendarPanel` の `EventList`/`EventSheet` に `readOnly`/`onDelete` を追加) (2026-07-13)
- タイムラインの**終日予定チップを上部の固定ヘッダにし、時間軸だけを内部スクロール**させるようにした(ユーザー要望)。当初 `position: sticky` で固定したが、スクロールバーがチップ帯の高さまで伸びて違和感があったため、タスク/予定パネルと同じ「見出し固定＋中身だけスクロール」構成に変更(`.panel--timeline .panel__body` を `overflow:hidden` の縦フレックスにし、時間軸を `.timeline__scroll` で内部スクロール)。これでスクロールバーは時間軸の範囲だけにかかり、終日チップは常に上部に見える (2026-07-13)
- カレンダー一覧(`calendarList`)の取得を1本の共有クエリに集約し、二重取得を無くした(旧 TODO #32)。従来は予定取得・月ドット・作成先一覧・アカウントメールがそれぞれ `calendarList` を叩き、5分ポーリングのたびに重複していた。共有クエリ `['calendar','list']`(`useCalendarList`、staleTime 30分)を1本立て、予定・月ドットはこの一覧を引数で受け取り、作成先一覧・アカウントメールは同じクエリの `select`(`toWritableCalendars`/`primaryEmail`)で導出する形にして、不要な API 呼び出し(クォータ・レイテンシ)と取得ロジックの散在を解消した (2026-07-13)
- Undo スナックバーの本文を**2行まで表示**するようにした(旧 TODO #38)。従来は1行省略(末尾「…」)で、「「(長いタスク名)」を削除しました」のような文言だと何を取り消すのか読み取りにくかったため、2行まで折り返して表示する(`-webkit-line-clamp`) (2026-07-13)
- ヘッダの設定歯車と、モーダルの閉じる×の**タッチターゲットを 44px 以上**に広げた(旧 TODO #36)。歯車(`.app__settings`)を約35px→44px 角、閉じる×(`.modal__close`)は×グリフの大きさは保ったまま透明な44px角の当たり判定を持たせ、指で押しやすくした(タスクのチェックと基準をそろえた) (2026-07-13)
- 空状態の文言を**文末に句点を付けて統一**した(旧 TODO #37)。「今後の予定はありません。」「受信トレイにメールはありません。」「タスクはありません。すべて順調です。」と句点・語調をそろえた (2026-07-13)
- タスクのクイック追加を、**入力欄・「今日」チェック・「追加」ボタンを1行**に並べる形にした(縦スペース節約・ユーザー要望)。従来は入力欄の下にチェックとボタンの行があり2段だったのを、`.tasks__add` を折り返し可能な横並び(`flex-wrap`)にまとめた(幅が足りない場合のみ折り返す)。あわせてチェックのラベルを「期限を今日にする」→「今日」に簡潔化 (2026-07-13)
- タイムラインで**予定の無い日を選んだときの初期スクロール位置**を、朝7時が画面上端付近に来る位置に調整した(ユーザー要望)。従来は基準時刻を画面の上から約4割に置いていたため、予定の無い日は早朝4時台から表示されて空白が目立っていた。予定のある日・今日はこれまでどおり(最初の予定/現在時刻を上から約4割に) (2026-07-13)
- 設定の「週の開始曜日（今月カレンダー）」の括弧書きを削除した(ユーザー指摘。月カレンダーは今月だけを表示するものではないため)。「週の開始曜日」に統一 (2026-07-13)
- タイトルバー中央の今日の日付を**年から表示する**ようにした(例「2026年7月13日 (日)」)(ユーザー要望)。文字列が少し長くなるため、中央表示を隠す狭幅の境界を 560px→640px に広げた (2026-07-13)
- 予定の作成シートで**過去の日付を選べないようにした**(旧 TODO #30)。開始日入力(`type="date"`)に `min`(今日)を設定し、過去日で作成した予定が一覧(今日以降)に出ずに消えたように見える混乱を防ぐ。編集時は既存の日付を尊重するため下限は付けない(進行中の複数日予定などを保つため) (2026-07-13)
- タイムラインパネルの見出しを、固定の「タイムライン」から**表示中の日を表す動的な見出し**に変更した(ユーザー要望)。今日・明日は「今日の予定」「明日の予定」と言い換え、それ以降は「M月D日(曜)の予定」と表示する(例「7月20日(日)の予定」)。見出しと重複するタイムライン上部の日付キャプション(`.timeline__date`)は削除した。見出しだけが選択日を購読する小コンポーネント `TimelineHeading` を用意し、日付切替で `App` 全体を再描画しないようにした (2026-07-13)
- 密度型1列目のパネルを「今日の予定」→**「タイムライン」に改名し、月カレンダーで選択した日の予定を表示**するようにした(既定は今日)(ユーザー要望)。月カレンダーの日付をクリックすると、その日の予定がタイムラインに表示される(あわせて従来どおり予定リストもその日へスクロール)。選択できる日は月カレンダーの範囲(今日〜約+34日)で、`useCalendarEvents` の取得範囲(今日〜+35日)に収まるため追加のAPI取得は不要(同じキャッシュを選択日でフィルタするだけ)。実装: 選択日を疎結合に共有する軽量ストア `selectedDate.ts`(`useSelectedDate`/`setSelectedDate`、既定 null=今日。日をまたいでも既定が当日を指すよう null を保持)を追加。`TodayTimeline` は選択日で予定を絞り、ドラッグ作成(#17)の日付も選択日にし、赤い現在時刻線は今日を表示中のときだけ出す。初回・日付切替時のスクロールは基準時刻(今日=現在時刻/他日=その日の最初の予定、無ければ8時)を可視域の上40%へ合わせ、5分ポーリングの再取得ではスクロール位置を奪わない(`scrolledForDateRef` で表示日が変わったときだけ再スクロール)。タイムライン上部に表示日の見出し(例「今日 ・ 7月13日 (月)」`.timeline__date`)、月カレンダーに選択中セルの枠強調(`.month__cell--selected`)を追加 (2026-07-13)
- 密度型(≥1340px)の列幅を調整した(ユーザー要望)。「今後の予定(7日)」列を少し狭め(`1.1fr → 1.0fr`)、そのぶん「今月(カレンダー)」列を広げ(`0.76fr → 0.86fr`)、「メール」列も少し狭めた(`1.7fr → 1.5fr`)。あわせて中間幅(960〜1339px)のメール列も少し狭めた(`1.4fr → 1.3fr`)(`index.css` の `grid-template-columns`) (2026-07-13)
- 密度型の24hタイムライン(`TodayTimeline`)で、左の時刻ラベルとドラッグ範囲(予定・作成の選択矩形)の間の隙間を詰めた(ユーザー要望)。ラベル用に確保する左マージン `GUTTER` を 40px → 34px に縮小("24:00" が収まる範囲でできるだけ詰めた) (2026-07-13)
- 密度型の24hタイムライン(`TodayTimeline`)の**初回スクロール位置を「最下部(夜)」から「現在時刻基準」に変更**した(TODO #29)。従来は初回に一番下(深夜)までスクロールしていたため、朝イチで開いても午前の予定が画面外で毎回上スクロールが必要だった。現在時刻の赤線(`nowRef`)が可視域の上から約4割の位置に来るように合わせ(スクロール親との `getBoundingClientRect` 差分で算出し、終日チップ行の高さ可変によるオフセットずれを回避)、午前の予定と現在時刻が最初から見えるようにした。5分ポーリングの再取得のたびにスクロール位置を奪わないよう初回だけに限定するガード(`scrolledRef`)は維持。従来は未使用(デッドコード)だった `nowRef` をこのスクロール基準に活用して解消した (2026-07-13)
- タスク・予定の**長いタイトルを編集画面を開かずに読める**ようにした(ユーザー要望)。一覧では従来どおり1行省略(末尾「…」)だが、行をタップして展開したとき(アクションバーが開くとき)はタイトル(予定は場所も)を折り返して全文表示する(`[aria-expanded='true']` のときだけ `white-space: normal`＋`overflow-wrap: anywhere`)。あわせて読み取り専用(編集不可)の予定もタップで展開できるようにし(従来は `disabled` でタップ不可だった)、共有カレンダー等の長いタイトルも読めるようにした(編集・削除ボタンは書き込み可能な予定のときだけ表示) (2026-07-12)
- 「今後の予定」パネルの「＋予定」ボタンを見出しと同じ行に並べた(ユーザー要望)。従来は見出し(`panel__title`)を App 側で描画し、その下のツールバーに右寄せでボタンを置いていたのを、見出しを `CalendarPanel` 内のツールバーへ移し、`justify-content: space-between` で「左に見出し・右にボタン」の1行にした(App 側の当該 `<h2>` は削除) (2026-07-12)
- 密度型(≥1340px)で「今後の予定」「カレンダー」パネル(上段)の高さをさらに少し低くした(ユーザー要望)。グリッドの行比を 上段 `0.6fr → 0.5fr`、下段(タスク)を `1.4fr → 1.5fr` に調整し、上段を低め・下段(タスク)を厚めにした (2026-07-12)
- カレンダー(月ミニカレンダー)上部の表示範囲キャプション「M/D 〜 M/D」を画面から消した(ユーザー要望。見れば分かるため不要)。代わりに、表示グリッドの**先頭セル(最初の日付)**を「M/D」表記にして月が分かるようにした(例「7/12」。従来からある月初1日の「M/1」表記も維持)。範囲文字列はスクリーンリーダー用の `aria-label` にのみ残し、`.month__caption`(CSS)は削除した (2026-07-12)
- メールパネル見出し横の「この端末で非表示」ボタンを撤去した(ユーザー要望。会社PC用に足した機能だが、自宅PCで誤って押すと再有効化に手間がかかるため)。同じ表示ON/OFFは設定モーダルの「メール（Gmail）を表示」トグルにあり、歯車を開く一手間があるぶん誤操作しにくい。メール見出しは他パネルと同じ素の `panel__title` に戻し、専用の `.panel__head` 見出し行(CSS)も削除した。あわせて、万一非表示にしても再有効化で毎回ログインを求められないよう、Gmail 有効化ボタン(`handleEnable`)を「すでに `gmail.modify` 同意済みの端末なら同意フローを挟まず即有効化」する挙動に変更(設定側トグルと同じロジック) (2026-07-12)
- 4カラムの密度型レイアウトに切り替わる画面幅の境界を **1200px から 1340px に引き上げた**(ユーザー要望。狭めていったとき4パネルが窮屈になる前に早めに3カラムへ移行させたい)。960〜1339px は3カラム、1340px 以上で密度型(4カラム)になる。CSS の `@media` の境界(`index.css`)と、密度型専用部品(24hタイムライン・天気)の描画判定に使う `WIDE_QUERY`(`useMediaQuery.ts`)を揃えて更新 (2026-07-12)
- 密度型のメールパネルの横幅を、広げすぎたため少し戻した(ユーザー要望)。列比のメール列を `1.9fr → 1.7fr` に狭め、他3列を微増(`0.66fr/1.1fr/0.76fr/1.7fr`) (2026-07-12)
- 密度型(≥1340px)のメールパネルの横幅を広げた(ユーザー要望)。天気・予定・今月をやや詰めてメール列を広げ、メール一覧・本文プレビューをより広く表示できるようにした (2026-07-12)
- 密度型(≥1200px)のタスクのバケツ(かんばん風の横並びカラム)の幅を、**列数によらず均一**にした(ユーザー要望)。従来は `flex: 1 1 170px`＋`flex-wrap: wrap` で基準幅170pxを目安に折り返していたため、バケツの数によってカラム幅が変わっていた。`flex: 1 1 0`＋`flex-wrap: nowrap` に変え、残り幅を全カラムで等分するようにした (2026-07-12)
- 選択中(本文を展開中)のメールが分かりやすいよう、その行の背景を薄いアクセント色で塗るようにした(ユーザー要望)。展開状態のとき Gmail 行に `gmail__item--open` クラスを付け、`color-mix` でテラコッタ8%の淡い背景＋角丸を付与(明暗どちらのテーマでも地に馴染む半透明) (2026-07-12)
- トップ画面のヘッダーから接続時刻の表示(「接続: HH:MM 取得（約1時間で失効）」)を削除した(ユーザー要望。実機での再ログイン頻度検証用の計測点だったが常用画面では不要なため)。`ConnectionStatus` は再接続が必要なときだけ「再接続」ボタンを出し、通常時は何も表示しない(`App.tsx` から `acquiredAt` の受け渡しを撤去) (2026-07-12)
- 設定モーダルが窮屈だったため各所の余白を広げた(ユーザー要望)。区画の上下パディングを 0.75rem→1.15rem、見出し・値・説明文の間隔を 0.4〜0.5rem→0.6〜0.7rem に拡大し、行の最小高さ・行間・プルダウン(テーマ/週開始)や地点入力欄のパディングと角丸も整えて全体をゆったりさせた (2026-07-12)
- デザインの世界観を「白地×青の実用重視」から**温かみ・親しみのある暖色基調**へ全面刷新した(ユーザー要望「事務的な印象を変えたい」)。方向性は「温かみ×テラコッタ/琥珀のアクセント×がっつり刷新」。(1)配色: 地・面・罫線・文字を青みから黄み寄りのニュートラル(クリーム地〜こげ茶)に置き換え、アクセントを青→テラコッタ(`--primary`、素焼きの器のような暖かいオレンジ赤)に変更。差し色に琥珀(`--amber`、金茶)を追加。意味的に寒色が要る箇所だけ `--cool`(青=最低気温・土曜)、「明日・これから」に `--success`(緑)を割り当て。危険色 `--danger` も暖色寄りの朱に。(2)角丸・影: 硬い罫線への依存をやめ、大きめの角丸(`--radius`/`--radius-lg`/`--radius-pill`)とやわらかい茶系の影(`--shadow-sm`/`--shadow-md`)でカードを軽く浮かせる方式に。パネルは角丸18px＋淡い枠＋影、ボタン・アクション・タブ選択・設定ボタンは丸み(pill/真円)に。(3)タイポ: 見出し・アプリ名・モーダル/シート見出しに丸ゴシック(`--font-display`)を適用して親しみを付与。着地先をプラットフォーム別に明示(macOS=Hiragino Maru Gothic ProN＝丸ゴシック / Windows 10・11=Yu Gothic UI / 旧 Windows=Meiryo / それ以外=system-ui)。丸ゴシックは Windows 標準に無いため丸みは macOS 限定の効果で、Windows では癖のない Yu Gothic UI で安定表示される(崩れない)。各パネル見出しの左に暖色グラデの短い縦バー、アプリ名の頭に暖色の丸いしるしを添えた。(4)操作フィードバック: ボタン/アクションにホバー時の暖色化と押下の沈み込み、選択中タブに暖色の淡い座布団を追加。(5)アクセシビリティ: キーボード操作時のフォーカス枠(`:focus-visible`、暖色)を全体に付与し、動きを減らす OS 設定ではトランジションを止めるようにした。※「今日を主役にする視覚階層(旧レビュー#3)」はユーザーの意向により見送り、全パネルは対等に扱う (2026-07-12)
- UI/UX レビューの指摘を反映してデザインの一貫性・アクセシビリティを改善した(6点)。(1)ダークモードでスナックバー(「元に戻す」通知)の Undo 文字色が読めない問題を修正: スナックバーは地色が明暗で反転するため、文字色も `light-dark(#7da2ff, #2563eb)` にして明暗どちらの地でも十分なコントラストを確保。(2)タスク完了チェック(丸ボタン)のタップ判定を透明な擬似要素(`.tasks__check::before`)で 44px 角に拡張(見た目の丸は 1.1rem のまま。指での誤タップ防止・Apple 推奨の 44pt 相当)。(3)場当たりに 0.66〜2rem まで十数種類あった文字サイズを 8段階のスケール(`--fs-2xs`〜`--fs-2xl`)に整理し、CSS 全体をトークン参照に統一(最小 0.72rem。判読しづらい 0.7rem 未満を廃止。装飾記号の × のみ実寸を残す)。(4)危険色の赤を `--danger` トークンに集約(直書き12箇所を置換)、青の直書き3箇所を `--primary` に統一、Gmail 行ホバーで未定義変数 `--surface-hover` を参照しダークで効いていなかったのを他と同じ `color-mix` 方式に修正。(5)タスク行の右端に淡い「›」を出し、タップでアクションが開くことを示す手がかりに(開くと下向きに回転)。月ミニカレンダーはホバー時に薄い枠を出してクリック可能と分かるようにした。(6)月カレンダーの過去日・当月外の日を `opacity` での減光から補助文字色(`var(--text-muted)`)に変更し、薄くしつつ最低限の判読性を保つようにした (2026-07-12)
- メールの「この端末で非表示」ボタンを、本文上部から**パネル見出し(「メール」)と同じ行**の右端へ移した(ユーザー要望)。見出し行を「見出し＋右寄せアクション」の横並び(`panel__head`)にし、ボタンは Gmail が有効表示中(`gmailActive`)のときだけ見出し横に出す。あわせて App 側の Gmail 有効判定を非反応的な `isGmailEnabled()` から購読フック `useGmailEnabled()` に変えて、非表示化が即座に反映される(見出しのボタンやタブの未読バッジが即消える)ようにした。GmailPanel 側のツールバー(`gmail__toolbar`)は撤去 (2026-07-12)
- パネル見出しを整えた(ユーザー要望): 「今日」→「今日の予定」、メールを「メール（受信トレイ・未読）」→「メール」だけに。メールは既読も表示するようになり「未読」表記が実態と合わなくなっていたため簡潔にした (2026-07-12)
- 天気パネルの上段(今日の天気)を中央寄せにした(ユーザー要望。左に寄って見えたため)。`.weather__now` に `justify-content: center` を指定(下段の明日・明後日は従来どおり `space-around` で均等配置＝左寄せではない) (2026-07-12)
- 今日の24hタイムラインの初期スクロール位置を「現在時刻付近」から「一番下(夜まで)」に変えた(ユーザー要望。下端でも活動時間帯は視野に入るため)。スクロールする祖先要素を探して初回に最下部へ移動させる(`getScrollParent`→`scrollTop = scrollHeight`) (2026-07-12)
- 密度型レイアウト(≥1200px)で天気パネルをメール(Gmail)の上から今日の24hタイムラインの上(1列目・上段)へ移した(ユーザー要望)。空いた4列目は Gmail が全3行を占めて大きくなり、あわせてメール列の横幅を少し広げた(列比 …/…/…/1.55fr)。天気は列が細くなるため、パネル内を縦2段に分け、上段=今日の天気(現在＋最高/最低)、下段=明日・明後日のミニ予報を横並びにするレイアウトへ変更(境目は上罫線) (2026-07-12)
- タスク・予定パネルの「上部の追加UI固定」を、本体全体を固定表示(sticky)する方式から「固定ヘッダ＋一覧だけスクロール」構成に変えた(ユーザー要望。sticky 方式ではスクロールバーが固定ヘッダの横まで伸びて違和感があったため)。パネル本体を縦フレックスにし、追加フォーム/「＋予定」は縮まないヘッダ、その下の一覧を内部スクロール領域(`tasks__scroll`/`calendar__scroll`)にした。これでスクロールバーが一覧の高さぶんだけになり、固定ヘッダの横に伸びない。広い画面(≥960px)でのみ適用 (2026-07-12)
- タスク追加フォームの「今日」ボタンを「期限を今日にする」チェックボックスに変えた(ユーザー要望。ボタンだけでは何をするものか分かりにくかったため)。入力欄の下に「期限を今日にする」チェックと[追加]ボタンを横並びで置き、チェック時のみ期限を今日にして追加する(挙動は従来の `dueToday` トグルと同じ) (2026-07-12)
- タスクの期限「移動先」ボタンをバケツごとに出し分けるようにした(ユーザー要望。明日タスクに「明日へ」等、自分と同じ日への無意味な移動が出ていたため)。期限切れ=今日へ/明日へ、今日=明日へ/来週へ、明日=今日へ/来週へ、以降=今日へ/明日へ、期限なし=今日へ/明日へ(`RESCHEDULE_BY_BUCKET`)。あわせて移動時のスナックバー文言を「今日へに移動しました」→「今日に移動しました」と自然にした (2026-07-12)
- 予定・タスクの取得範囲を今後30日から**先5週間（35日）**に広げ、ミニカレンダーの表示範囲と揃えた(ユーザー要望)。これでカレンダー上でクリックできる日（今日〜グリッド末尾。最大で今日+35日）の予定が必ず一覧に載り、日付クリックでその日へスクロールできる。共通定数 `UPCOMING_DAYS` を 30→35 に(予定取得・作成後の一覧内判定・タスクの「以降」上限で共用)。あわせて予定パネルの見出しを「予定（今後30日間）」→「今後の予定」に、空表示を「今後の予定はありません」に、範囲外作成時のトーストを「5週間より先のため一覧には表示されません」に変更(日数の中途半端な表記を避ける) (2026-07-12)
- タスクのバケツから「今週」を無くした(ユーザー要望)。バケツを 期限切れ/今日/明日/今週/以降/期限なし から **期限切れ/今日/明日/以降/期限なし** の5つにし、明後日〜30日先はすべて「以降」に集約。これに伴いタスク側の週開始設定への依存(今週末の算出)も不要になり削除した (2026-07-12)
- 天気パネルの「今日（現在の天気）」がやや窮屈だったため間隔を広げた(ユーザー要望)。絵文字・気温・情報のブロック間隔(`.weather__now` の gap を 0.6→0.9rem)、現在ブロックとミニ予報の間隔(`.weather` の gap を広め)、情報3行の行間(line-height)を調整した (2026-07-12)
- タスク行で、今日・明日・期限なしバケツは日付欄(`.tasks__due`)を描画しないようにした(ユーザー要望。これらは日付を出さないため空欄ぶんの余白＝タイトル左のインデントが不要)。行レイアウトを grid(`auto 1fr auto`＝常に日付列を確保)から flex に変え、日付は期限切れ/今週/以降のときだけ挿入する。期限のあるバケツ内では `.tasks__due` の `min-width` で日付左端をそろえる (2026-07-12)
- 天気パネルで明日・明後日のミニ予報の右に余白が空いていたのを、ミニ予報ブロックを残り幅いっぱいに広げ(`flex:1`)2日を均等配置(`justify-content: space-around`)して埋めた(ユーザー要望) (2026-07-12)
- 密度型の上段(予定・カレンダー)の高さをもう少し小さくした(ユーザー要望)。行比の上段を `0.7fr→0.6fr`、下段(タスク)を `1.3fr→1.4fr` に調整し、上段を低め・下段を厚めにした (2026-07-12)
- ミニカレンダーを「今月」表示から「今週から5週間」表示に変更した(ユーザー要望。月初が過去になりがちで、予定パネルは今日以降しかスクロールできず過去日を出す意味が薄いため)。グリッドの左上を今週の開始曜日(週開始設定を尊重)にし、そこから5週=35セルを描画。今日より前の日(今週内の過ぎた曜日)は薄く・クリック不可(`div` 描画)にし、今日以降だけクリックで予定パネルへスクロールできる。月をまたぐことがあるためキャプションは表示範囲(M/D 〜 M/D)にし、月初(1日)のセルは「M/1」表記で月替わりの目印にした。パネル見出しは「今月」→「カレンダー」。グリッド行を6→5週に調整 (2026-07-12)
- 密度型のタスク多列カラムの間に縦の仕切り線を入れた(ユーザー要望。どのチェックボックスがどのバケツのものか分かりにくかったため)。各カラムに `border-left`＋左余白を付け、先頭カラムはパネル左端に揃えて線・余白なし。`.tasks__groups` を `align-items: stretch` にして短いバケツでも仕切り線が下まで通るようにした (2026-07-12)
- PC 密度型レイアウト④(タスク期限バケツ多列)。タスクパネルの分類を従来の4グループ(期限切れ/今日/今後30日/期限なし)から、期限で細かく分けた6バケツ **期限切れ / 今日 / 明日 / 今週 / 以降 / 期限なし** に変更し、密度型(≥1200px・下段で2列ぶんの幅)ではこのバケツを横並びの多列カラム(かんばん風)にした(狭い幅では従来どおり縦積み)。(1)分類は保存値ではなく毎回 `due` から導出(`bucketTasks`)。「今週」は端末の週開始設定(`useWeekStart`・日曜/月曜始まり)を尊重して今週末を求め、今日/明日の翌日〜今週末を「今週」、それ以降〜30日を「以降」に振り分ける。「以降」は予定パネルと揃えて今後30日までに限定(30日より先は非表示)、期限切れ・期限なしは日数に関係なく全件表示。(2)多列は `.tasks__groups` を `flex-wrap`(`flex-basis:170px`＝おおむね4列目安)で表現し、中身のあるバケツだけ描画するので空カラムは出ない・入りきらない分は折り返す。縦スクロールはパネル本体(`panel__body`)側のまま。(3)日付が自明でないバケツ(期限切れ/今週/以降)でだけ各行に期限(M/D)を表示。バケツ見出しに色(今日=プライマリ / 明日=青緑)を追加 (2026-07-12)
- 予定パネル・タスクパネルの表示期間を「今後7日」から「今後30日」に広げた(ユーザー要望。パネルを固定高さ+内部スクロールにしたため表示エリアが際限なく伸びる心配が無くなったのが背景)。カレンダー取得(`fetchUpcomingEvents`)と作成後の一覧内判定(`isWithinUpcomingWindow`)を共通定数 `UPCOMING_DAYS=30` に統一、予定パネル見出し「今後30日間」、タスクの「今後」グループも `UPCOMING_DAYS=30` と見出し「今後30日」に更新 (2026-07-12)
- 24hタイムラインを終日 0:00〜24:00 まで描画し、早朝(7:00以前)・深夜(23:00以降)へもパネル内スクロールで到達できるようにした(ユーザー要望)。従来は活動時間帯(7-23)に絞って範囲外は自動伸縮のみだったのを、全時間帯を常に描画する方式に変更。既定表示位置は従来どおり現在時刻付近(初回に現在時刻へスクロール) (2026-07-12)
- 密度型の列幅を微調整(ユーザー要望)。予定(今後7日)を少し広く・今月を少し狭くし、`0.7 : 1 : 1 : 1.3` → `0.7 : 1.2 : 0.85 : 1.3`(タイムライン : 予定 : 今月 : Gmail)にした (2026-07-12)
- 密度型レイアウトの上下段の高さ配分を調整(ユーザー要望)。上段(予定7日・今月)は今月がほぼ正方形のカレンダーで高さを要さないため、密度型の行比を `auto:1fr:1fr` から **`auto:0.7fr:1.3fr`** に変え、上段を約3割低く・下段(タスク)を厚くした (2026-07-12)
- PC 密度型レイアウト①-b(固定高さ+内部スクロール・ユーザー要望)。項目数(予定・タスク・メールの件数)で各パネルの縦が伸び全体がガタつく問題を解消するため、広い画面(≥960px)では**ダッシュボード全体を画面高にぴったり収め、ページ全体はスクロールさせず各パネル内だけをスクロール**する方式にした(Fable レビューで成立を確認・必須修正を反映)。(1)高さの連鎖 `.app(100dvh・縦flex)→.app__main(flex:1)→.panels(flex:1・grid)→.panel(縦flex)→.panel__body(flex:1・overflow-y:auto)` を組み、各段に `min-height:0` を通した。各パネルの見出し(`.panel__title`)は固定し、中身を新設の `.panel__body` ラッパー(全幅で常時描画=親不変で再マウントなし)に入れて内部スクロールさせる。(2)密度型のグリッドを2行→**3行**にし、行スパンで「天気は小さい auto 行(上)・Gmail はそのすぐ下の2行スパンで大きく」を両立(タスクと同じ高さから始めない・ユーザー要望)。(3)予定(今後7日)と今月の列幅を 1.4:1→**1:1** に変更(ユーザー要望)。(4)fr トラックが中身で膨らむ罠を避け行定義を `minmax(0,1fr)` に、`≥960px` で `body{overflow:hidden}`・`height:100vh→100dvh` フォールバック・天気カードに `max-height` の保険を追加。スマホ(≤959px)は従来どおりページスクロール(据え置き) (2026-07-12)
- PC 密度型レイアウト①(骨格スライス)。2026-07-12 のグリルで合意した「段積み3カラム」の器を用意した(中身の新規部品は次スライス以降)。(1)従来の「2状態(960px 境の3カラム/タブ)」を**3状態**に拡張し、幅ごとに `grid-template-areas` で配置を切り替える方式にした(`index.css`): 〜959px はスマホ(タブで1枚)、960〜1199px は予定・タスク・Gmail の3カラムリスト(従来相当)、1200px〜は密度型(段積み3カラム)。(2)密度型は Fable 助言に基づき、状態を持つ3パネル(予定7日リスト・タスク・Gmail)は**単一インスタンスのまま**エリア名で再配置し(再マウントを避け Gmail 展開状態やスクロールを保持)、新規の読み取り専用3部品(24hタイムライン・月カレンダー・天気)だけ `matchMedia` で広幅時のみ条件描画する。DOM は `grid-template-areas` が効くようフラットな6グリッドアイテムにした。(3)画面幅判定用の小フック `useMediaQuery`(`useSyncExternalStore` ベース)と、次スライスで実装する新規部品のプレースホルダー(`TodayTimeline`/`MonthCalendar`/`WeatherPanel`)を追加。密度型では横幅制限を 1100px→1600px に緩めた (2026-07-12)
- レイアウト仕上げ(#2 の一部・ユーザー要望)。(1)予定のカレンダー名(主カレンダーはメールアドレス)とタスクのリスト名の「出典名」を既定で非表示にした。個人利用では出典名がノイズになりやすいため。設定モーダルの「この端末の表示」に「予定・タスクの出典名を表示」トグルを追加し、ON にすると従来どおり表示する(端末ローカル設定。`displayPrefs.ts` の購読可能ストア `useShowSourceLabels`/`setShowSourceLabels`、既定 false)。(2)タスクの「今後」グループを今日から7日先までに絞り、見出しを「今後7日」に変更した(予定パネルの「今後7日間」と範囲を揃える。`UPCOMING_DAYS=7` で `dueStr` を上限日付と文字列比較。期限切れ・期限なしは日数に関係なく全件表示のまま)。(3)PC の3カラムの幅比を等幅から「予定1 : タスク1 : メール1.4」に変え、本文プレビューがあり情報量の多いメール列を広くした (2026-07-12)
- レイアウトを「PC=3カラム並列 / スマホ=タブ切替」の2状態に整えた(フェーズ5・器のスライス、Fable 助言)。(1)従来の「800px以上で2列」を廃止し、960px を境に、以上は3カラム(予定・タスク・メールを横並び)、未満はタブで選んだ1枚だけ表示にした。(2)3パネルは常に全てマウントしたまま、スマホでは非選択パネルを CSS で `display:none` にするだけの方式にしたため、タブを行き来してもスクロール位置や展開状態が保持され、裏側の定期取得(ポーリング)も途切れない。(3)スマホは画面下端固定のタブバー(予定/タスク/メール)で切り替える。ホームバー領域を避けるため `env(safe-area-inset-bottom)` を確保し、本文がバーに隠れないようメインに下余白を追加。(4)タブ切替時は `window.scrollTo(0,0)` で先頭に戻す(ページ全体スクロール方式で前タブの位置が引き継がれるのを防ぐ)。(5)初期表示タブは「予定」固定(朝イチで今日の予定を最初に見る想定。前回タブの記憶はしない)。件数バッジ(未読・期限切れ)は次スライスで追加予定 (2026-07-12)
- Android の PWA 本体でメール本文中のリンクを押したとき、アプリ内の Chrome(Custom Tab)ではなく Chrome 本体で開くようにした(ユーザー要望)。スタンドアロン起動の PWA では外部リンクが既定で Custom Tab に流れるため、Android かつスタンドアロン起動のときだけ(`ANDROID_STANDALONE` 判定)、リンクの遷移先を Android の intent URI(`intent://…;scheme=https;action=android.intent.action.VIEW;end`、`toIntentUrl`)に変換して Chrome 本体を起動する(best-effort)。iframe 内から直接 intent を発行すると sandbox にブロックされうるため、`allow-same-origin` を利用して本文 iframe のリンククリックを隔離枠の外(非sandbox の本体側)で横取りし、本体側に一時的な `<a>` を作って intent を発行する方式にした(PWA 自体は開いたまま)。Mac や Android の通常タブでは従来どおり `target="_blank"` で新規タブに開く。URL のフラグメント(#以降)は intent 構文と衝突するため変換時に落とす(メールでの利用は稀)。`mailto:` 等 http/https 以外は変換せず既定動作に任せる (2026-07-12)
- カレンダー予定の作成・編集フォームに「場所」と「メモ(詳細)」の入力欄を追加した(フェーズ4-c の仕上げ)。予定の `location`/`description` を読み書きできるようにし、一覧では場所があれば予定行に「📍場所」を小さく表示する。patch では空文字を送れば場所・メモを消去できる。時刻に加えて場所・詳細も登録できるようになった (2026-07-12)
- アクセストークンの保存先をメモリのみから **sessionStorage** に変更した(ユーザー判断)。デスクトップ(Mac/Windows)のブラウザで、ページ更新や再訪問のたびに毎回ログインが必要だった問題への対処。起動時に sessionStorage の有効なトークン(TTL 55分)を復元して即接続する経路(`restoreSession`)を主とし、無ければ従来のサイレント認証→明示ログインの順にフォールバックする。sessionStorage はタブを閉じると消えるため「端末に長期保存しない(localStorage を避ける)」方針は概ね維持。デスクトップでサイレント認証(`prompt:''`)がサードパーティCookie制限で機能しなかったのが背景(Android アプリ版はプロセス保持でトークンが生き残るため元々再ログイン不要だった) (2026-07-12)
- `PLAN.md` にプロUI/UXデザイナー視点+シニアエンジニア視点のレビュー結果を反映。(1)誤操作対策(完了・アーカイブ・削除の Undo、操作の3層配置)、(2)スマホタブのバッジと初期タブ、(3)例外状態(未ログイン/空/エラー/読み込み中)の設計、(4)再ログイン頻度の現実的な見積もりへの修正と段階的認可の採用、(5)メール本文表示のセキュリティ対策(サニタイズ+隔離枠+画像ブロック)、(6)API実装の落とし穴メモ、(7)縦切り実装の段取り、などを追記。`TODO.md` に #6〜#8 を追加 (2026-07-12)

## Fixed

- **未読メールを開いて閉じると、画面がそのメールの位置まで勝手にスクロールしてしまう**不具合を修正した(ユーザー指摘)。未読メールは開いた瞬間に既読化され、一覧が「未読が上・既読が下」に並び替わって元の行が下方へ移動する。モーダルを閉じたとき `useDialog` がフォーカスを元の行ボタン(=移動後の位置)へ戻す際、素の `focus()` がその要素を画面内へ入れようとしてスクロール位置が飛んでいた。フォーカス復帰を `focus({ preventScroll: true })` にし、フォーカスは戻しつつ画面位置は動かさないようにした (2026-07-24)
- カレンダー予定の**楽観的更新(作成/編集/削除/復元の即時反映)が画面に効いておらず、サーバー往復後の再取得まで反映が遅れていた**不具合を修正した。`#54` で予定一覧クエリのキーに「表示中カレンダー構成の署名」を足して3要素(`['calendar','upcoming', 署名]`)にしたが、ミューテーション側の書き換えは2要素の `CAL_KEY`(`['calendar','upcoming']`)のままだった。TanStack Query の `setQueryData`/`getQueryData` は**キー完全一致**でしか当たらないため、楽観的更新が誰も購読しない“幽霊エントリ”に書かれ、画面には反映されていなかった(`cancelQueries`/`invalidateQueries` は前方一致なので効いていた)。前方一致で実エントリを引く `getQueriesData` に変え、その実キーへ書き換え・巻き戻し(`rollback`)するようにした。これが「タイムラインでドラッグ確定時に一瞬元位置へ戻る」現象の根因でもあった (2026-07-15)
- タイムラインで**予定をドラッグ移動/リサイズして指を離した瞬間、確定前に一瞬だけ元の位置に戻る**ちらつきを修正した(ユーザー指摘)。指を離す `eventPointerUp` でプレビュー用 state(`evDrag`)を即 `null` に戻していたが、楽観的更新でキャッシュが新位置になるのは次のマイクロタスク(`onMutate` の `await cancelQueries` 後)なので、その1フレームだけ「プレビュー無し＋旧位置キャッシュ」で描画され元位置へ戻って見えていた。`evDrag` に `phase`(`'drag'`=掴み中 / `'commit'`=保存中)を持たせ、離した後も確定位置のプレビューを保持し続け、保存完了(`onSettled`、成功/失敗どちらでも)で解除するようにした(掴み中の装飾は `phase==='drag'` のときだけ付け、保存中は位置だけ維持。失敗時はキャッシュが巻き戻り解除と同時に元位置へ戻る) (2026-07-15)
- タスク編集シートの**選択中の期限チップの文字が読めなくなる**問題を修正した(ユーザー要望)。選択中チップの背景に明るいアクセント色 `--primary` を使い白文字を載せていたためコントラストが不足していたうえ、`.btn:hover` が文字色を `--primary`(背景と同系色)に変えるため、ホバー時は文字が背景に溶けて消えていた。白文字とのコントラストを確保した専用背景色 `--primary-btn` に変え、`.btn--primary` と同様にホバー時も背景を濃く(`--primary-btn-strong`)しつつ白文字を維持するようにした(`.tasks__add-today.is-on`) (2026-07-15)
- PWA の更新トースト「更新して再読み込み」ボタンが**狭い画面で押せなかった**のを修正した。トーストが横1行の flex で、テキスト(`.update-toast__text`)が縮まずボタンが `flex-shrink:0` だったため、内容が `max-width` を超えて画面右外へはみ出し、更新ボタンが画面外に出て押せなかった。折り返し(`flex-wrap:wrap`)を許可してテキストを伸縮可能にし、ボタンが必ず画面内に収まるようにした(あわせて更新ボタンに枠を付け押せると分かりやすくした)。加えて、`updateSW(true)` が controllerchange を受けずリロードしない環境向けに、`applyUpdate` へ「未登録なら即リロード／2秒以内にリロードしなければ強制リロード」の保険を追加した (2026-07-14)

- 密度型の24hタイムラインで、**短い予定（15〜30分など）のタイトルが縦に切れて読めない**問題を修正した(ユーザー要望)。予定ブロックの高さが2行に満たない（約32px未満）ものを「短い予定」と判定し、**色枠（ブロック）自体を中身に合わせて下へ伸ばす**（固定 `height` ではなく `minHeight`）ことで、時刻＋タイトルを1つの色面の中に収めて全文表示する（`timeline__event--short`。伸びたぶんが下の予定に隠れないよう `z-index` を上げる）。通常の高さの予定は従来どおり固定高さ＋枠内クリップのまま。子要素（時刻・タイトル）には `flex-shrink:0` を付け、flex が枠内へ圧縮して切るのを防ぐ。当初はタイトルだけに別背景を敷いて枠外へはみ出す実装だったが、**枠の色と文字背景の色がわずかに食い違って見える**ため（ユーザー指摘）、枠自体を伸ばして同一色の1面にする方式へ変更した。Chrome ヘッドレスで 15/30/60 分の予定を描画し、短い予定の枠が中身に合わせて伸び色が均一なこと・通常予定が枠内に収まることを目視確認 (2026-07-13)
- **日本語メール本文の文字化け（`◆◆◆` など）を修正**した(ユーザー報告)。原因は Shift_JIS / EUC-JP / ISO-2022-JP のメールを、charset 宣言の欠落・誤記のまま UTF-8 固定で復号していたこと。本文の生バイトを複数の文字コード候補で試し、**文字化け(U+FFFD)が最少のものを自動採用**する方式にした(`decodeBody`)。(1)ISO-2022-JP はエスケープ(ESC$/ESC()をバイト列で検出して最優先。(2)UTF-8 を宣言 charset より先に試す(UTF-8 検証は厳格なので「実体 UTF-8 なのに誤って Shift_JIS 宣言」等の誤採用を防ぐ・Fable 助言)。(3)スコアは U+FFFD を重く、半角カナ(EUC-JP を Shift_JIS 誤読した兆候)を軽く加点し、EUC-JP が Shift_JIS に化けるのを防ぐ。(4)base64url は `atob` 前にパディングを補完。UTF-8/Shift_JIS/EUC-JP/ISO-2022-JP・誤ラベル各ケースの復号をユニットテストで確認済み (2026-07-13)
- **#50** 一部の一覧取得が `Promise.all` のままで、**1件の一時的な失敗でパネル全体がエラー**になる不具合を修正した。Hacker News の記事20件取得を `mapPool`(同時8件・部分失敗許容)化して成功分だけ表示し(全滅時のみエラー)、Gmail 受信トレイの未読/既読リスト取得も `Promise.allSettled` にして、**既読リストだけ失敗しても未読は表示**するようにした(既読失敗時はコンソール警告。未読側の失敗は「未読0件」の誤表示を避けるため従来どおり失敗として扱う)。各メール本文/メタの取得は元から `mapPool` で部分失敗許容 (2026-07-13)
- **#54** カレンダーの表示 ON/OFF や追加・削除をしても**予定リスト・月ドットに反映されない**不具合を修正した。`useCalendarEvents`/`useMonthEventDays` の `queryKey` に「選択中カレンダー(`selected!==false`)の id を並べた署名」を含め、構成が変わると自動で取り直すようにした(従来はキーが `['calendar','upcoming']` 固定で、間隔/フォーカス再取得まで古いカレンダー集合のまま残ることがあった) (2026-07-13)
- 設定モーダルを上下中央寄せから**上端固定**に変更し、タブを切り替えるたびにモーダル全体が上下に動く問題を解消した(ユーザー要望)。中央寄せ(`align-items: center`)だとタブごとに中身の高さが変わるとモーダルが再センタリングされて動いていた。`.modal-overlay` を `align-items: flex-start`＋上部余白(`padding: 8vh 1rem 1rem`)にし、高さの差は下方向にだけ伸縮させることで、タイトル・タブの位置を固定した (2026-07-13)
- Google 認証（GIS）のトークン要求が同時に走ったときに、解決先が入れ替わって先発がタイムアウトに倒れる/後発が誤ったトークンで解決しうる競合を修正した(旧 TODO #31)。GIS のコールバックが単一の resolver（`pendingResolve`/`pendingReject`）を共有する仕組みのため、`gisClient.ts` の `doRequest` に「同時に1つ」ガード(進行中なら新規要求を即エラー)を入れ、解決・拒否・タイムアウトのいずれでも共有変数を確実にクリアする `finish()` に集約した。起動時のサイレント認証とユーザー操作の競合を UI の `initializing` ゲートだけに頼らずコードで保証する (2026-07-13)
- メール本文の文字コード対応(旧 TODO #12)。本文デコードが UTF-8 決め打ちで、`ISO-2022-JP`(JIS)等の日本語メールが化けうる問題を、パーツの `Content-Type` の `charset` を読んで `TextDecoder` を切り替える形にした(`Shift_JIS`・`EUC-JP` 等も対応。未知/壊れた charset は UTF-8 にフォールバック)(`gmail/api.ts`) (2026-07-13)
- タイムラインで**終日の予定をクリックしても編集シートが開かない**問題を修正した(ユーザー報告)。時刻あり予定はクリックで編集できる(#18)のに、上部にチップ表示される終日予定は `<span>` のままでクリックできなかった。終日チップも、書き込み可能で確定済みのものはクリック/Enter で編集シートを開けるようにした(`timeline__chip--editable`。読み取り専用・追加直後は従来どおり操作不可)(`TodayTimeline`) (2026-07-13)
- Gmail 未有効時の案内文が実態と合っていなかったのを修正した(ユーザー指摘)。「有効にすると受信トレイの未読を表示します。」としていたが、実際は受信トレイの未読・既読の両方を表示するため、「有効にすると受信トレイのメールを表示します。」に改めた(`GmailPanel`) (2026-07-13)
- 進行中の複数日予定が「今後の予定」で**過去日の見出しの下に表示される**問題を修正した(旧 TODO #34)。一覧は今日以降しか出さないのに、昨日開始・明日終了のような進行中予定は実開始日(昨日)でグループ化され、過去日の見出しが一覧最上部に現れていた。`groupByDay` を「実開始日が今日より前なら今日のグループに寄せる」方式に変更し、進行中予定を今日の見出しの下にまとめた。あわせてグループの日付キーと `data-date` をゼロ埋め(`YYYY-MM-DD`)に統一し、React キーだけゼロ埋めなしで2系統になっていた点(旧 TODO #47)も解消した(`CalendarPanel`) (2026-07-13)
- ダークテーマでメール本文(HTML)の背景が純白で固定され眩しかった問題を軽減した(旧 TODO #27)。メールHTMLの互換性のため本文色(暗色)は反転させず、ダーク時のみ本文カードの地を純白から穏やかな暖色オフホワイト(`#e7e0d3`)にして眩しさを抑えた(暗色文字とのコントラストは十分維持)。あわせて iframe 枠(`.gmail__frame`)の読み込み前・余白の背景をテーマ色(`var(--surface)`)にして白い縁がダークで浮くのを緩和。ダーク判定は実効テーマ(手動テーマ or OS 追従)を見る `useEffectiveDark` を追加(`buildSrcDoc` にダークフラグを渡す) (2026-07-13)
- 最重要ボタン(白文字×テラコッタ)のコントラスト不足を修正した(旧 TODO #24)。アクセント色 `--primary` は明るめで白文字とのコントラストが不足(特にダークで約3.3:1)していたため、**ボタン背景専用色 `--primary-btn`(`#b0521c`)/`--primary-btn-strong`(`#9e4a24`)を新設**し `.btn--primary` に適用、白文字で WCAG AA(4.5:1)以上を明暗とも確保した(実測 5.16:1 / ホバー 6.07:1)。アクセント色自体(今日の強調・枠など)は従来のまま (2026-07-13)
- 補助テキスト色(`--text-muted`)のコントラストが WCAG AA を下回っていた問題を修正した(旧 TODO #23)。時刻・スニペット・期限などの小さな文字に広く使う色で、明色は surface 上 3.75:1・bg 上 3.39:1 と不足していた。明色を `#8c8175`→`#6f665b` に一段濃くし、surface 上 5.5:1・bg 上 5.0:1 と AA(通常文字 4.5:1)を満たすようにした(暗色 `#a89c8b` は既に AA 達成のため据え置き) (2026-07-13)
- 天気パネルの絵文字の字形が不揃いになる問題を修正した(ユーザー報告)。`🌤🌫🌦🌧🌨⛈` などは既定が「文字表示(text presentation)」のため、異体字セレクタ(U+FE0F)が無いと Windows 等でモノクロの字形になり、`☀️⛅☁️` などのカラー絵文字と混在して見えていた(特に現在の天気アイコン)。天気コード→絵文字の変換を `rawWeatherCodeInfo` と、末尾に U+FE0F を付与する `colorEmoji` に分離し、すべての天気絵文字をカラー(絵文字)表示に統一した(既に U+FE0F を持つものは二重付与しない)(`weather/api.ts`) (2026-07-13)
- HTML形式メールのプレビュー内でスクロールがひっかかる問題を修正した(ユーザー報告)。HTMLメールは `<iframe>`(`gmail__frame`)で表示し、iframe の高さは読み込み時に中身の高さへ**一度だけ**合わせていた。HTMLメールは画像や Web フォントが後から読み込まれて中身が伸びるため iframe が短いままになり、iframe の内部に縦スクロールが生まれて、その内部スクロールがマウス/指のスクロールを奪い親パネルのスクロールが止まっていた(プレーンテキストは `<pre>` 表示でこの問題は起きない)。高さの追従を一度きりの測定から `ResizeObserver`(要素サイズの変化を監視する仕組み)による常時追従に変更し、iframe に `scrolling="no"` を付けて iframe 自身のスクロールを無効化(スクロールは親パネルへ流す)することで解消。あわせて画像後読みで本文が切れる問題(旧 TODO #46)も解消した(`GmailPanel.tsx` の `HtmlBody`) (2026-07-13)
- Gmail を有効化した端末で、ヘッダ/バナーの「再接続」を押すと Gmail 表示が壊れ、タスクまで巻き添えで停止する不具合を修正した(旧 TODO #20)。原因は、再接続・追加同意・初回ログインの全入口を担う `App.tsx` の `handleConnect` が常に `INITIAL_SCOPES`(カレンダー+Tasks のみ)を要求しており、設定モーダルの再接続だけが Gmail 込みに分岐していたこと。Gmail 権限を持たない新トークンになると Gmail が 403(権限不足)を返し、グローバル単一フラグ `needsScope` が立って `useTasks` が停止するため、タスクまで「追加の許可が必要」表示で止まっていた。対策: (1)要求スコープの判断を全接続入口で共通の `desiredScopes()`(`auth/scopes.ts`。Gmail 有効かつ gmail.modify 同意済みなら Gmail 込み)に集約。起動時の事前準備・サイレント認証も同関数に統一。(2)Gmail エンドポイント由来の 403 では `needsScope` を立てないようにし(`ScopeError` に呼び出し先 URL を持たせ、`queryClient` の共通ハンドラで Gmail の 403 を除外)、Gmail の権限不足がタスク/カレンダーを止めないようにした (2026-07-13)
- 予定・タスク・メールの一覧取得を並列取得(`Promise.all`)で束ねていたため、1件の一時的な失敗で一覧全体がエラー表示になる問題を修正した(旧 TODO #21)。`Promise.all` は1つでも失敗すると全体が失敗するので、メール1通が取得直後に削除されて 404、あるいはカレンダー/リストの1つが一時的に 429(レート制限)・5xx を返しただけで、受信トレイ/全予定/全タスクがまるごとエラーになっていた。共通ユーティリティ `mapPool`(同時実行数の上限つき並列実行・各要素の成功/失敗を個別保持)/`fulfilledValues`/`throwIfAllRejected`(`google/pool.ts`)を追加し、Gmail の `fetchInbox`・カレンダーの `fetchUpcomingEvents`/`fetchEventDaysInRange`・タスクの `fetchAllTasks` を、成功分だけ表示する部分失敗許容に変更。全件が失敗したとき(例: 認証切れ 401・権限不足 403)だけはその理由を投げ直し、従来どおり再接続/追加同意の共通UXへ流す。あわせて Gmail の `messages.get`(最大50件)は同時実行を6件までに絞り、ユーザー毎秒クォータ(レート制限)に触れにくくした (2026-07-13)
- Gmail 一覧で日本語の差出人名・件名が `=?UTF-8?B?…?=` のように文字化けする問題を修正した(旧 TODO #22)。Gmail API の `messages.get(format=metadata)` はヘッダを RFC 2047「エンコードワード」形式のまま返すため、日本語メールでは差出人・件名が化けていた。ヘッダ表示前に `=?charset?B|Q?text?=` を charset ごとに復号する処理(`decodeHeader.ts` の `decodeMimeWords`。Base64/Quoted-Printable の両方式に対応、charset は `TextDecoder` に委ねるので UTF-8 だけでなく ISO-2022-JP・Shift_JIS 等も復号可、隣接エンコードワード間の空白詰め・復号不能時は原文フォールバックも実装)を追加し、`fetchMessageMeta` の From/Subject に適用した。本文の文字コード対応(TODO #12)とは別問題(あちらは本文、これはヘッダ) (2026-07-13)
- Android アプリ表示でステータスバー/ジェスチャー(ナビ)領域だけ背景色が違って見える問題を直した(ユーザー報告)。これらの領域の色は PWA の `theme-color`(と manifest の `theme_color`/`background_color`)で決まるが、旧デザインの青(`#2563eb`)のまま暖色刷新に追従できていなかった。(1)`index.html` の `theme-color` メタをアプリ背景(暖色)に合わせ、`media` 属性でライト(`#f7f1e8`)/ダーク(`#1c1815`)を OS 設定に応じて出し分けるようにした。(2)manifest の `theme_color`/`background_color` も暖色(`#f7f1e8`)に更新。(3)アプリ内のテーマ手動切替(端末設定に合わせる/ライト/ダーク)にも追従するよう、`applyTheme` が両メタの `content` を選択テーマの色で上書きする(`system` 時は本来の明暗色に戻す)ようにした (2026-07-12)
- メール本文中の URL がただの文字列になって押せないことがある問題を修正した(ユーザー報告)。(1)プレーンテキストのメールは `<pre>` にそのまま出していたため URL が非リンクだったのを、本文中の URL を検出してリンク化(`tokenizeLinks` で URL とテキストに分割し `<a target="_blank" rel="noopener noreferrer">` を挿入、改行・空白は `<pre>` で保持)。(2)HTMLメールでも `<a>` で囲まれていない生の URL はリンク化していなかったため、サニタイズ後に `<a>`/`<style>`/`<script>` 外のテキストノードを走査して生 URL を `<a>` に置換(`linkifyBareUrls`)。URL 判定は http(s):// と `www.` を対象に、末尾の句読点や日本語の全角文字・括弧は URL に含めない(ASCII のURL構成文字のみを本体とする)。HTML本文は従来どおり iframe 内なので、Android では既存のクリック横取り→intent 起動もそのまま効く (2026-07-12)
- スマホ表示で全タブのパネル左に不自然な余白が出る問題を修正した。パネルへの `grid-area`(エリア名)割り当てをグローバル(全幅共通)に置いていたため、`grid-template-areas` を定義しないスマホでは未定義の列名を参照し、ブラウザが空の暗黙カラムを生成してしまっていた。エリア名の割り当てを `@media (min-width: 960px)`(= `grid-template-areas` が存在する広い画面)の中だけに移し、スマホは 1fr 単一カラムに素直に積むようにした(密度型レイアウト①から潜在していた) (2026-07-12)
- PC の3カラム表示でメールの列だけ横に広がりすぎ、等幅(1fr)が崩れる問題を修正した。CSS グリッドの列は既定で「中身の最小幅」より狭くなれず、メール本文(iframe や幅広の表・折り返さないテキスト)がある列だけ中身に押し広げられていた。各パネル(`.panel`)に `min-width: 0` を付けて中身より狭くなれるようにし、3列を等幅に揃えた。あわせて差出人(`.gmail__from`)にも `min-width: 0`+`flex: 1` を付け、フレックス内で省略記号(…)が正しく効くようにした (2026-07-12)
- Android のリンク intent 対応の切り分け用に一時追加していた診断表示(本文上の「リンクは Chrome で開きます」目印、Gmail ツールバーのビルド版・Android 判定の表示)を撤去した。Android 実機では新コードが反映されず診断表示自体が出なかった＝PWA(WebAPK)のキャッシュで旧版が動き続ける問題(`TODO.md` #15)が原因と判明したため、リンク intent の実機検証は #14 として保留し、UI を元に戻した。intent 起動のコード自体は Mac では無効(`IS_ANDROID` が false)で無害なため残す (2026-07-12)
- Android の PWA でメール本文のリンクがまだアプリ内(Custom Tab)で開く問題への対処。(1)適用条件を「Android かつスタンドアロン起動」から「Android なら適用」に広げた。WebAPK では `display-mode: standalone` が期待どおり返らずリンク横取り自体が発動していなかったため(`ANDROID_STANDALONE`→`IS_ANDROID`)。(2)intent URI に `package=com.android.chrome`(開き先を Chrome 本体に固定=Custom Tab 回避の要)と `S.browser_fallback_url`(未解決時のフォールバック)を追加。(3)切り分け用に、Android のときだけ本文上に「リンクは Chrome で開きます」の目印を一時表示(判定が効いているか実機で確認するため。動作確定後に外す) (2026-07-12)

- 権限不足（403 insufficient_scope）を認証切れ（401）と区別して扱うようにし、タスク機能を追加する前の「カレンダーだけの許可」で使い続けている端末（主に Android アプリ版。プロセスが生き続けて再ログインが起きないため古い許可のまま更新されない）で「タスクの取得に失敗しました」と一般エラーになっていたのを修正。(1)`fetchJson` が 403 の `WWW-Authenticate: insufficient_scope`／本文の `ACCESS_TOKEN_SCOPE_INSUFFICIENT` を検出して専用の `ScopeError` を投げる。(2)`authStore` に `needsScope` 状態と `markNeedsScope()` を追加し、`QueryCache`/`MutationCache` の onError で `ScopeError` を拾って追加同意を促す状態に切り替える。(3)ログイン成功時にも、必要なスコープ(`INITIAL_SCOPES`)に不足があれば API 失敗を待たず先回りで `needsScope` を立てる。(4)画面上部に「タスクを表示するには追加の許可が必要です」バナー＋「許可する」ボタンを出し、押すと段階的認可で不足スコープだけを追加同意する（再ログイン不要）。同意後は `needsScope` が解けてタスク取得が自動復活。(5)権限不足中はタスク取得を止め(`enabled` に `!needsScope`)403 の叩き続けを防止。(6)復元経路でも起動時に必ず GIS を先読みし、「許可する」クリックで同期的にトークン要求できるようにした (2026-07-12)
