// 今日の24時間タイムライン（密度型レイアウトの1列目）。
// 活動時間帯（既定 7:00〜23:00 など）に絞り、範囲外に予定がある日だけ自動で伸縮。
// 現在時刻の横線・終日予定はチップ表示。読み取り専用（クリックで既存の編集UIを開く予定）。
// → スライス③で本実装。現在は骨格確認用のプレースホルダー。
export function TodayTimeline() {
  return <p className="panel__note">（24時間タイムラインはこのあとのスライスで実装します）</p>
}
