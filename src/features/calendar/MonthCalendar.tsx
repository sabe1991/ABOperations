// 月ミニカレンダー（密度型レイアウトの2列目・上段の右）。
// 今日を強調し、予定のある日に小さなドット。読み取り専用（v1 ではクリック遷移なし）。
// → スライス②で本実装。現在は骨格確認用のプレースホルダー。
export function MonthCalendar() {
  return <p className="panel__note">（月カレンダーはこのあとのスライスで実装します）</p>
}
