// 天気（密度型レイアウトの3列目・上段）。
// Open-Meteo（APIキー不要・無料）で、設定に入れた地点の今日の天気・気温を小さく表示。
// → スライス⑤で本実装。現在は骨格確認用のプレースホルダー。
export function WeatherPanel() {
  return <p className="panel__note">（天気はこのあとのスライスで実装します）</p>
}
